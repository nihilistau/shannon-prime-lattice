---
type: contract
title: CONTRACT-CHAT-FULLSTACK — bring the served /v1/chat onto the full Shannon-Prime substrate
description: "Pre-registration: route the daemon's served gemma-4-12B chat off the vanilla forward+decode onto the real stack — fast resident gemma4_kv decode + L2 sampler (Stage A), then byte-exact + XBAR ring/recall/replay + ARM + NIGHTSHIFT (Stage B). Each stage gated, bit-exact-when-off."
tags: [contract, chat, xbar, abi, daemon, gemma4]
timestamp: 2026-06-18T00:00:00Z
resource: shannon-prime-system-engine/tools/sp_daemon
sp_status: ACTIVE
sp_gate: G-CHAT-A1+A2
sp_commit: engine 91b4177
sp_repro: run_console.bat ; curl -N -X POST :3000/v1/chat
---

# CONTRACT-CHAT-FULLSTACK — the served chat on the real substrate

**Status: PRE-REGISTRATION (DRAFT), 2026-06-18.** Operator chose **A + B**. This contract scopes the whole program, contract-first (prompt.md §7 rule 2), and is the single place the chat↔substrate integration is gated. Parents: `PPT-LAT-L1-ABI-v0.md` (the L1 ABI: §6 forward, §6b kvdecode verb), `CONTRACT-BYTEEXACT-forward.md` (SP_BYTEEXACT), `CONTRACT-XBAR-P3-ring-on-exec.md` (ring/spill/SWA/LSH-recall/SP_REPLAY), `CONTRACT-XBAR-C2-memo-curator-loop.md` (#222 replay/rewind), `CONTRACT-XBAR-R3-consolidation.md` (Ring-3 / NIGHTSHIFT), `CONTRACT-KAIROS-K0-K1.md` (persistent-KV ABI). Terminology per prompt.md §7.4 (ABM/ARM/Ring 1/2/2′/3, Spinor, Frobenius, PPT, LAT).

## 0. The finding this contract responds to (audited, file:line)

The daemon's served `/v1/chat` (`tools/sp_daemon/src/routes.rs` `v1_chat` + `run_kvdecode_chat`) runs a **vanilla forward+decode** through the L1 session ABI. Layer status on the served path TODAY:

| Layer | Status | Evidence |
|---|---|---|
| fast CUDA decode (`gemma4_kv_decode_logits`) | **registered but SLOW** — chat takes `run_kvdecode_chat` (routes.rs:322-330) → `kv::decode_step` → `gemma4_kv_decode_logits`, but measured **0.27 tok/s** (per-step velocity path, no CUDA-graph; cf. paper-06 graph decode 26 tok/s) | routes.rs:417-564; daemon log `register_kvdecode_backend OK` |
| byte-exact (`SP_BYTEEXACT`) | DORMANT (off-by-default null floor; never set on the chat path) | cuda_forward.cu; CONTRACT-BYTEEXACT |
| XBAR P3 (ring/spill/SWA-ring/LSH-recall/`SP_REPLAY`) | DORMANT (the resident cache is a plain full KV cache; no `SP_XBAR_*`/`SP_ARM_*` on chat) | cuda_forward.cu overlays; only `test_gemma4_cuda` drives them |
| ARM two-ring + canonical `decode.c` | DORMANT (qwen3/CPU path + harnesses only) | core/forward/decode.c |
| Ring-3 / NIGHTSHIFT | NOT-WIRED (offline host-Python + native core/ring3) | tools/ring3, core/ring3 |
| sampling | **greedy argmax**, hardcoded in the HTTP handler | routes.rs:566 `fn argmax`, called at :499/:544 |

**Symptoms:** decode loops (`<image|>` attractor) = greedy argmax + no turn-EOS for this artifact; 0.27 tok/s = the resident decode is not on the fast (graph) kernels. Both must be fixed at the right layer, not as `routes.rs` hacks.

## 1. Non-negotiables (inherited)

- **Bit-exact-when-off / null floor.** Every new path is a flag that is a strict no-op by default; the one-shot `gemma4_decode_cuda` and the existing `gemma4_kv_*` byte-exact gates stay byte-identical. No citable gate (paper-06 26.1 tok/s @ PPL 5.12, G-BYTEEXACT-FORWARD-12B, G-WIRE-CUDA-DECODE-GEMMA4 32/32==oracle) may regress.
- **Pre-registered gates**, thresholds pinned BEFORE code. **No silent gate revision** — surface upstream + amend this contract.
- **Scope travels with every number.** Honest negatives stay attached.
- **The ABI grows append-only.** Any new verb registers in `PPT-LAT-L1-ABI-v0.md` first.

## 2. STAGE A — chat on the fast decode substrate (the prerequisite for B)

### A1 — fast resident decode (the speed)
**Problem:** `gemma4_kv_decode_logits` on the resident `sp_g4_kv` cache runs the per-step *velocity* path (0.27 tok/s). The fast 12B decode (paper-06, 26 tok/s) is the **CUDA-graph** decode in `gemma4_decode_cuda`.
**Work:** make the resident-KV decode use the fast kernels/graph (capture the jagged gemma4 decode topology over the persistent cache, or route the resident decode through the same graph path the one-shot decode uses — without breaking the persistent-KV semantics or the O(1) rewind). Pin GPU clocks for the timing leg (2060 mem clock is unlockable — within-leg slope only, cf. `feedback_pin_clocks_for_tests`).
**Gate G-CHAT-A1 (pre-registered):** on the real 12B via `/v1/chat`, decode ≥ **15 tok/s** sustained (target ≥20; floor well above the 0.27 scalar/velocity), AND token-identical output to the current `run_kvdecode_chat` greedy path on a fixed prompt (the speedup is kernel/graph, not a numerics change), AND `G-WIRE-CUDA-DECODE-GEMMA4` still 32/32==oracle. Falsify: any token divergence, or < 10 tok/s.

### A2 — L2 sampler (the looping)
**Problem:** greedy argmax in the HTTP handler → `<image|>` loop; no temperature/top-p/repetition-penalty; the gemma4-12b-b1 artifact has no `<end_of_turn>` *token* (control toks `<|turn>`=105 / `<turn|>`=106; turn-end is a stop-*string*).
**Work:** a sampler module at the L2 decode boundary (the kvdecode ABI deliberately returns full-vocab logits so "L2 owns sampling", cuda_kvdecode_dispatch.rs:139) — temperature, top-p, top-k, repetition/frequency penalty, seedable RNG. Plumb the knobs through `ChatRequest` (+ defaults) and surface them in the console (`console.html`). Keep the turn-stop-string handling; greedy (temp=0) stays bit-deterministic.
**Gate G-CHAT-A2 (pre-registered):** on a fixed set of ≥5 regression prompts (incl. the two that looped), temp=0.7/top-p=0.95/rep-pen=1.1 produces coherent, non-looping completions that terminate at the turn boundary; temp=0 == the current greedy output bit-for-bit (determinism preserved). Falsify: any loop, or temp=0 nondeterminism.

**Stage A exit:** `/v1/chat` is fast (≥15 tok/s), coherent, non-looping, on the resident `gemma4_kv_*` substrate — the doorway B builds on.

## 3. STAGE B — the full stack on chat (each a flag-gated overlay on the A substrate)

All B overlays attach to the **same resident `gemma4_kv_*` decode** A lands on; each is off-by-default (null floor) and individually gated.

### B1 — byte-exact chat (`SP_BYTEEXACT`)
Route the resident decode through the exact-integer islands+attention (CONTRACT-BYTEEXACT). **Gate G-CHAT-B1:** flag-off == A output byte-identical; flag-on == run-to-run bit-identical, PPL parity; expose as a `ChatRequest`/console toggle ("auditable mode"). (This is the AUDITABILITY axis on chat — exact, reproducible generation.)

### B2 — XBAR ring + recall + replay on chat
Make the resident chat cache the **XBAR ring** (SWA-ring `pos%Wring` + compact global slab + learned-LSH sparse global recall, P3.2-b-2b) so chat is O(1)-VRAM in context; wire the C2 curator recall (`SP_REPLAY`/#222) so prior episodes can be recalled into a live chat turn. **Gate G-CHAT-B2:** ring-on == ring-off output token-identical at small context (the proven null), VRAM flat across a context doubling (the O(1) ladder), and a planted needle survives (NIAH) — reusing the P3 gates on the live chat path. Surfaces the `SP_XBAR_SWA_*`/`SP_ARM_*` knobs through the session (ABI addendum if needed — register in L1-ABI first).

### B3 — ARM two-ring memory on chat
Bring the ARM two-ring (Ring-1 working / Ring-2 verbatim Spinor-KV, the math-core `core/arm`) onto the gemma4 chat decode (today it's the qwen3/CPU path only). **Gate G-CHAT-B3:** Ring-2 spill/recall byte-exact under poison on the live chat cache; bit-exact-when-off.

### B4 — NIGHTSHIFT between turns
Run the (now native-C `core/ring3`) consolidation loop **between chat turns / on idle**: aging Ring-2 turns → Ring-3 gist under the irreversible G-R3-LOSS gate; recall-from-both on the next turn. **Gate G-CHAT-B4:** an unattended multi-turn session consolidates with net-positive gated promotions, zero canonical corruption, full receipt log (the N1 soak, applied to chat). Recall-time gist upsampling stays FORBIDDEN (R3 §1 trap).

## 4. Build order (smallest falsifiable steps)

A1 (fast decode) → A2 (sampler) → **Stage-A exit gate** → B1 (byte-exact toggle) → B2 (XBAR ring/recall) → B3 (ARM) → B4 (NIGHTSHIFT). B2–B4 may surface new session-level knobs; each registers in `PPT-LAT-L1-ABI-v0.md` §6c+ BEFORE building. Every stage: pin the gate here, build, run on the real 12B via the console, land the receipt + commit, update STATE/SESSION-HANDOFF.

## 5. Run-records

**STAGE A — CLOSED GREEN (2026-06-18, engine `91b4177`, lineage `1375e65→91b4177`; +632/−15, 6 files).**

- **G-CHAT-A2 (sampler / the loop) — GREEN.** New `tools/sp_daemon/src/sampler.rs` (temperature, top-k, top-p, repetition + frequency penalty over generated history, seedable SplitMix64); knobs in `ChatRequest` + a controls row in `console.html`; both decode loops route through `Sampler::sample` (old `fn argmax` → `sampler::argmax`). The `<image|>` attractor is killed by suppressing the image-placeholder control token to −∞ on the **sampled path only** (`tokenizer.suppress_token_ids()`, id-agnostic via `id_to_bytes`) — so **temp=0 reproduces the prior greedy output byte-for-byte** (SHA-equal incl. the original loop; the greedy null floor is untouched). 5 regression prompts (incl. the two that looped) → coherent, non-looping, turn-terminated; seeded sampling reproducible (seed 999 A==B). 4 sampler unit tests pass.
- **G-CHAT-A1 (fast decode) — GREEN on the floor + an honest scope correction.** Added off-by-default CUDA-graph resident decode (`SP_G4_KV_GRAPH=1`, `g4_kv_launch_full` captured once / replayed; position kernels read device `dpos` so one capture replays at any position) — **bit-identical armed or not, `G-WIRE-CUDA-DECODE-GEMMA4` 32/32==oracle both ways**. **Measured 15.4–15.9 tok/s** (slope, prefill cancelled, SM pinned 1605; 2060 mem clock unlockable). **Root cause correction:** the resident decode is **memory-bandwidth bound** (9.4 GB Q4B dp4a GEMV/token), NOT launch-bound — graph-on == graph-off tok/s. The contract's "0.27 tok/s" premise was an artifact of `/v1/metrics` (`tokens_decoded/uptime`, a lifetime average over idle+load), NOT the decode rate; the served path with the standard daemon env was already ~15 tok/s. ≥15 floor cleared; ≥20 target needs sustained mem boost the 2060 won't pin. The graph overlay is retained as a correct null-floor-safe path; **bandwidth is the ceiling on this box, so the B-stage VRAM/O(1) (XBAR ring) work is the higher-leverage axis here.**
- **Null floor preserved.** No closed gate regressed. Daemon live on :3000 (run_console env). Receipts: engine `_a1_*`/`_a2_*`/`_gate_*.log`.

**STAGE B1 — CLOSED GREEN (2026-06-18, engine `66e30bc`, math-core/submodule `307fdf2`).** Per-session byte-exact "auditable mode" toggle on `/v1/chat`. **Investigation:** the resident decode (`g4_kv_step` → `gemma4_kv_decode_logits`) already routed the 4 nonlinear islands through `d_bx_flag`, but the resident-decode **attention** was the gap (`k_attn_decode_win_dyn`/`_ring` had no bx branch — only the one-shot forward dispatched `k_attn_decode_win_bx`), and `d_bx_flag` was env-at-init only. **Built (additive, null-floor):** `gemma4_kv_byteexact_set(s,on)` runtime setter (`cudaMemcpyToSymbol` d_bx_flag + host `bx_on`); `g4_kv_step` routes attention → `k_attn_decode_win_bx` when on (host-ctx kernel, graph declined under bx — chat is per-step); `bx_on` calloc'd 0 = null floor. **ABI registered FIRST:** `sp_l1.h §6c` (append-only) documents the backend-internal kvdecode knob (not a new frozen `sp_session` verb). Daemon: `ChatRequest.byteexact` (default false) set under the resident-cache Mutex with an RAII reset guard; `console.html` "auditable" checkbox. **Gate G-CHAT-B1 GREEN (real 12B, temp=0):** OFF == Stage-A byte-identical (3 prompts, SHA-matched); ON run-to-run **bit-identical** (2 prompts); OFF-after-ON returns to the OFF reference (no flag leak); `G-WIRE-CUDA-DECODE-GEMMA4` 32/32==oracle, VRAM Δ=0. Honest scope: the resident decode has no PPL harness, so the citable 4.6665/4.6569 parity stays the one-shot forward's; B1's claim is determinism + parity + null-floor, not a re-derived PPL. Receipts `tests/fixtures/chat_fullstack/G-CHAT-B1{,_postB1}.log`.

**STAGE B2 — RED / STOPPED (2026-06-18). The SWA ring on the resident chat decode breaks coherence; its "GREEN" was a false positive caught by independent verification.** Built (engine `e425af0`, math-core `4cc0ebd`, ABI `sp_l1.h §6d`): SWA-ring startup knob (`SP_DAEMON_KVDECODE_RING_W`) routing `g4_kv_step` attention through `k_attn_decode_ring`, + per-request `gemma4_kv_replay` (`ChatRequest.replay`). **What actually happened:** with the ring ARMED, `/v1/chat` produces multilingual control-token soup (`子<audio|> ద relem<image|>…`) on *every* prompt incl. trivial raw continuation; with the ring OFF the model is **coherent** ("A prime number is a positive integer greater than 1…", "Paris"). So `k_attn_decode_ring` on the resident decode computes wrong attention. **The B2 agent's leg-1 "null-parity GREEN" (ring-OFF SHA == ring-ON SHA) was a FALSE GREEN** — a determinism SHA cannot distinguish coherent from garbage, so it passed bit-identical garbage; the leg-2 "O(1) VRAM" and replay legs are real but moot while the ring output is wrong. **LESSON (gate methodology): a coherence/quality gate is REQUIRED for any chat-decode change — determinism/null-parity SHAs are necessary but NOT sufficient; they certify reproducibility, not correctness.** ACTION TAKEN: the ring is **DISARMED in `run_console.bat`** (commented out) so the served default is the coherent full-cache B1 path; B2 stays OPEN pending (a) a real `k_attn_decode_ring`-on-resident-decode debug and (b) a pre-registered **coherence gate** (exact-match on a fixed factual-prompt set, ring-ON == ring-OFF *semantically*, not just SHA). The per-request `replay` plumbing is retained (independent of the ring). Independent diagnostic: `_diag_noring.log` + the ring-on/off transcripts above.

**A2-polish (follow-on, surfaced by the B2 diagnosis):** the control-token suppression covers only `<image|>` (not `<audio|>`/`<|turn>` etc. — extend the set, sampled path); and turn-stop never fires because this artifact's tokenizer has **no `<end_of_turn>` token** (it has `<|turn>`/`<turn|>`) so the model runs on past the turn — needs a turn-stop heuristic (e.g. stop on `<|turn>` / a double-newline + "Question:" guard). Chat is usable (coherent) without these; they are quality polish.

**A2-POLISH — CLOSED GREEN (2026-06-18, engine `cc4e26c`, daemon-only).** Clean coherent text chat + auto-stop, COHERENCE-gated (not SHA). (1) `tokenizer.suppress_token_ids()` now masks ALL pipe-wrapped control markers to −∞ id-agnostically (decoded bytes start `<`, end `>`, contain `|` → 19 ids on this vocab: `<|turn>`/`<turn|>`/`<image|>`/`<audio|>`/… + named specials + `<unusedN>`), on BOTH sampled and temp=0 paths; zero false positives verified (`<div>`/`<eos>` survive). (2) `ChatRequest.raw_logits` (default false) opts out of suppression → the byte-exact/B1 determinism null-floor reference (raw reproduces the old `<image|>` greedy loop bit-identical, sha F8F2C197…). (3) Turn-stop: artifact has no `<end_of_turn>`, so `<|turn>`/`<turn|>` (`turn_stop_ids()`) are treated EOS-equivalent in both decode loops + `default_stops()` extended with new-turn stop-strings. **Gate (real 12B, 6 factual prompts, temp 0 + 0.7):** all coherent+correct ("Paris"/"Tokyo is the capital of Japan."/"A prime number is a positive integer…"/"4"/"William Shakespeare"/"Bonjour"), ZERO control-token artifacts, all self-terminated at the turn boundary; independently re-verified by the orchestrator. 12/12 unit tests. Null floor intact (raw_logits), no closed gate regressed, B2 ring left DISARMED. Receipt `tests/fixtures/chat_fullstack/G-CHAT-A2-POLISH.log`. **→ the served text chat is now clean + usable; next = ring-fix (coherence-gated).**

**B2 RING-FIX — BLOCKED (2026-06-18); the ring is exonerated, a deeper coherence-fragility surfaced.** Trying to gate the ring, the agent found the soup is NOT the ring: a clean rebuild of the daemon CUDA backend from the SAME HEAD (`cc4e26c`) yields incoherent output even with the ring OFF, while the pre-session binary (same HEAD) was coherent. ROOT CAUSE: for the gemma chat-template prompt, the model's **rank-1 next token is a SUPPRESSED reserved/control id (258882)**; the visible answer rides entirely on the **rank-2+ logit ordering**, which is NOT build-stable (CUDA FP codegen reorders near-tied logits across rebuilds). **Every gate we have is blind to this** — `32/32==oracle`, byte-exact run-to-run, and even argmax all check only rank-1; they pass while rank-2+ (the actual visible text) flips coherent↔garbage. So the A2/A2-polish "coherent chat" was riding on a build-fragile sub-argmax. IMPLICATION: the fragility traces to a **malformed chat format** — gemma's `<start_of_turn>`/`<end_of_turn>` template uses special tokens THIS artifact lacks, so the model's true top prediction is a reserved token we then suppress (the band-aid). The single-entry inject_seq path (B5), done with the correct continuous-vector representation + formatting for this artifact, is plausibly the REAL coherence fix, not a reorg. Also found (real, latent, NOT the soup): the SWA-owner undo-journal hard-fails at `j>=Jmax` (chat never calls `gemma4_kv_commit` → dies past ~64 tokens), and per-request `kv::rewind(pos)` reads the journal OOB past `Jmax` on the ring path — fix by resetting via `gemma4_kv_reset` (counter reset, no journal replay) + auto-advancing `commit_pos` on overflow. Ring attention math (`k_attn_decode_ring`, slot `(s0+j)%Wring`) matches the proven one-shot G-P3-R2.b-2a for in-window contexts. STATE: tree clean at `cc4e26c` (nothing shipped), ring DISARMED, no daemon left up (refused to leave garbage behind a green). NEW GATE REQUIRED: a **rank-2..N coherence assertion** in the decode gate so rebuilds can't silently flip. Receipt `tests/fixtures/chat_fullstack/G-CHAT-B2-RING-DEBUG.log`. **Re-sequenced: stabilize coherence root-cause (build determinism + chat-format/suppressed-top-token) + add the coherence gate → B5 single-entry → ring (with journal/rewind fixes).**

**STAGE S1 — COHERENT + REPRODUCIBLE GEMMA4 CHAT — GREEN (2026-06-18, engine `58b6c2d`, daemon-only).** Fixed the operator-flagged "gemma3 template on a gemma4 model." ROOT CAUSE (from the artifact, not guessed): the `gemma4-12b-b1` vocab has **NO `<start_of_turn>`/`<end_of_turn>`** — its turn tokens are `<|turn>`=id105 (start) / `<turn|>`=id106 (end/eos); the old `gemma3_template` emitted the literal `<start_of_turn>…` strings which (gemma4 BPE `n_spec==0`) shattered into junk → off-format prompt → rank-1 = a suppressed soft token → the coherent↔garbage FP-reorder flip. SECOND BUG: `<turn|>`=106 (the stop token) was being masked by the id-agnostic pipe-control suppression → model could never stop. FIX (L2-only, no `.cu`/`sp_l1`/submodule): `apply_template_ids()` builds the prompt at TOKEN level with the real ids (`<bos>(<|turn>{role}\n{content}<turn|>\n)*<|turn>model\n`); un-suppress `<|turn>`/`<turn|>` + add `<turn|>` to `eos_ids`; **config-driven** `suppress_tokens`/`eos_token_id` from the model's `generation_config.json` (union, id-agnostic backstop); and **byte-exact integer decode is now the DEFAULT chat path** (build-independent determinism → kills the FP-reorder fragility; `raw_logits`/`byteexact:false` opt out). **Gate G-CHAT-S1 (measured TEXT):** 6/6 factual prompts coherent+correct (Paris/4/Shakespeare/blue/Jupiter/Hola — was garbage), **byte-identical across two separate builds** (reproducible by construction), zero control-token artifacts; orchestrator-verified live (Paris / "7 times 8 is 56." / black-hole / run-A==run-B). HONEST NEGATIVE: 2/6 answer correctly then run on at greedy (+ mild repetition) — the OK_Q4B QAT-b1 artifact's turn-discipline ceiling, bounded by max_tokens + structural stop-strings, NOT a stack bug (not hacked behind a green). lib 57/57; no closed gate regressed; B2 ring stays disarmed. Receipt `G-CHAT-S1.log`. **→ the chat is coherent, correct, reproducible, gemma4-correct, byte-exact-by-default. Foundation for B5 (single entry point).**

**STAGE B5 — SINGLE LATENT ENTRY POINT — GREEN (2026-06-18, engine `18a5f78`→`398232b`, submodule `cb601e9`, ABI `sp_l1.h §6e`).** The operator's image-1 architecture proven on the metal: text/audio/memory all enter through ONE residual seam (`gemma4_kv_inject_seq`), model mints K/V natively. New engine `gemma4_kv_inject_tokens(s,toks,n)` stages `embed_tokens[id]×√E` (E=3840, `embscale=sqrtf(E)`) device-side with the SAME arithmetic as the stock embed step, AND steps the **real token id as the placeholder** so the per-layer-embedding (PLE) gather matches prefill — the parity hinge. `ChatRequest.single_entry` routes the prompt through it (default off = prefill null floor); `inject_frames`+`inject_ph` exposes the raw E-float residual channel for the AUDIO (EAR/KAI-3) + MEMORY sources. **Gate G-CHAT-B5 (real 12B, temp=0, byte-exact default):** text-via-seam == prefill **6/6 BIT-IDENTICAL** (SHA-matched + text-read: Paris/4/Shakespeare/blue/Jupiter/Hola), single_entry run-to-run bit-identical, `inject_frames` accepts+digests a valid E=3840 frame (ragged rejected), no closed gate regressed (decode_step/`g4_kv_step` byte-untouched — B5 additive symbols only). Orchestrator-verified live (single_entry=="Paris."==prefill; "Gravity… curvature of spacetime…" coherent). Caveats: parity gated at temp=0 (6/6 SHA → sampling identical too); `inject_frames` smoke used a synthetic frame (real audio/memory-as-residual wiring = named follow-on; B5 delivers the channel); `gemma4_kv_replay` kept alongside (§6d-b) — the inject channel can also carry decoded episode residuals. **REPO HYGIENE FLAG:** standalone `shannon-prime-system` was BEHIND the engine submodule (had §6c only, missing §6d/B2); agent committed §6e to it (`300d32c`) but it needs rebasing onto the submodule lineage `cb601e9` (the canonical, ahead copy the engine builds against). Receipt `G-CHAT-B5.log`. **→ THE SINGLE ENTRY POINT IS REAL: reading a word, (future) hearing a sound, and recalling a memory are the same operation to the model's core.**

**STAGE B2 RING — CLOSED GREEN (2026-06-19, engine `7eb7231`), coherence-gated, RE-ARMED.** The O(1)-context SWA ring on the served chat decode is fixed. ROOT CAUSE (beyond the two diagnosed lifecycle bugs): arming the ring silently dropped the 40/48 SWA layers onto the **float** `k_attn_decode_ring` kernel, losing S1's byte-exact FP-reorder immunity → the soup. The slot math `(s0+j)%Wring` was correct all along. FIXES (engine-only, sp_l1 untouched): (1) **new `k_attn_decode_ring_bx`** — the exact-integer dual-prime dot + FB30 softmax with the ring slot remap, dispatched when `ring_W>0 && bx_on`, so the SWA layers stay build-deterministic under the ring; (2) **journal auto-advance `commit_pos`** on `j>=Jmax` (chat never commits → decode died past ~64 tok) — slides the journal to the recent Jmax-1 ticks (forward chat never rewinds mid-turn; curator/#222 callers commit, unaffected); (3) **per-request reset via `gemma4_kv_reset`** (no journal replay) instead of `rewind(pos)` (which read the journal OOB past Jmax on the ring). **Gate G-CHAT-B2-RING (real 12B, W=1024, byte-exact default) — 3 legs GREEN:** (1) coherence — Paris/4/Shakespeare + a ~160-token paragraph completed coherently PAST 64 tokens; (2) null parity — ring-ON == ring-OFF byte-identical (SHA-matched) AND coherent both sides; (3) **O(1) VRAM — ~10–20 MiB across a 6k→12k context doubling** (O(N) would add ~2–3 GiB; globals full-cache caveat). `G-WIRE-CUDA-DECODE-GEMMA4` 32/32==oracle, ring-OFF rebuild reproduces S1/B5. Orchestrator-verified live (ring-ON: "Paris." + 886-char coherent water-cycle past 64 tok). **Ring RE-ARMED in `run_console.bat` — the served default is now the O(1) ring path.** Receipt `G-CHAT-B2-RING.log`.

**═══ STATUS: the served `/v1/chat` is COHERENT, REPRODUCIBLE, BYTE-EXACT-DETERMINISTIC, O(1)-CONTEXT, and runs on the SINGLE LATENT ENTRY POINT. S1 + A2-polish + B1 + B5 + B2-ring all GREEN. REMAINING (next stages, not blocking): B3 ARM two-ring on the gemma4 decode; B4 NIGHTSHIFT between turns; wiring the real AUDIO source (EAR/voxtral) + memory-as-residual into the B5 `inject_frames` channel. ═══**

## 6. ARCHITECTURE DIRECTIVE — the single latent entry point (operator, 2026-06-18)

**The governing principle for all of Stage B and beyond:** the model has ONE true input — a stream of continuous latent vectors in its residual space — and **every modality enters through the one proven seam, `gemma4_kv_inject_seq`** (the KAI-2/KAI-3 "voice mechanism," residual-entry override → model mints K/V natively → RoPE + per-layer variance correct *by construction*). Three SOURCES feed that one entry:
- **text** → BPE ids (`sp_tokenizer_encode`) → `embed_tokens × √E` → inject_seq. (BPE doesn't disappear; it becomes the text→latent *projector*, beside the EAR audio projector. KAI-2's EMB control already PASSED 2/2 injecting real token embeddings through this seam — so this is proven, not speculative.)
- **audio** → EAR (GNA) frames → KAI-3 projector → inject_seq.
- **episodic recall** → `gemma4_kv_replay` (the ONE deeper seam: K/V cache, not entry — recalled memory, not fresh input; phase-exact placement, magnitude correct by construction; do NOT use the m=0 / σ-π^k math — that contradicts the proven path).

**Why this matters (no-spin):** it collapses the two-path (BPE-prefill vs residual-inject) design into one auditable channel; text and audio interleave seamlessly; and it is the architecture the byte-exact container was built to make safe. **The A2/ring fixes below are entry-agnostic** (they live in the shared decode loop / SWA cache), so they remain valid and come first.

### STAGE B5 — unify chat input onto the single inject seam
Re-route the daemon's text chat input from token-id `prefill_chunk` to **`gemma4_kv_inject_seq` over `embed_tokens(ids)×√E`** (the residual seam). **Gate G-CHAT-B5:** inject-path output is semantically equivalent to (ideally bit-identical to, else coherence-equal + documented delta) the prefill path on a fixed prompt set at temp=0; coherence gate (real factual prompts answered correctly); null-floor (prefill path remains available + untouched). Then the console + daemon expose all three sources (text / audio-frame / episode-recall) through the one entry. New session verb (e.g. `sp_session_inject_embeds`) registers in `sp_l1.h` append-only FIRST.

**STAGE B — REMAINING ORDER (operator-authorized): A2-polish (clean coherent text, coherence-gated) → B2-ring debug + coherence gate (the O(1) win) → B5 single-entry unification + surface the proven replay/inject channels in the console → B3 ARM → B4 NIGHTSHIFT. Each registers any new session knob in `sp_l1.h` first; each gates on COHERENCE, not just determinism (the B2 false-green lesson).**
