# CONTRACT XBAR-C1-lite — Memo v0, the heuristic curator loop on the qwen3 CPU two-ring

**Parent:** RFC-XBAR v1 §5 (C1-lite) + §7 (NIGHTSHIFT). **Status:** SPEC + seam analysis (2026-06-09). The parallel track to P2.b — needs no cloud, no 12B, no training; runs on the proven qwen3 CPU two-ring (C2.2, `T_ARM_GENKV` green).
**One line:** stand up the full **propose → gate → promote/rewind** control flow of the curator on the existing two-ring, with a heuristic Memo v0 (cold-evict / merge) and the gate = PPL delta on the consolidated episode — before any learned curator (P2.b adapter = C2) or CUDA-ring port (P3).

---

## 1. Seam analysis — three findings from the 2026-06-09 survey (read before coding)

The survey of `core/forward/decode.c` + `core/arm/arm.c` + `include/sp/arm.h` surfaced three architecture facts that reshape C1-lite's first slice. None were obvious; all are load-bearing.

1. **The two-ring is per-decode-EPHEMERAL.** `qwen3_generate_kv` / `qwen3_ppl_decode` build the recall index `projk` (`[NL × P × NKV × r]` f32) and the Ring-2 store *fresh inside the call* and `free()` them at `done:`. There is **no persisted episode object** for a curator to sit between. The curator's whole premise — operate on stored state between sessions — has no seam yet. *This is exactly RFC §7's "episode persistence" item, now confirmed as a prerequisite, not an add-on.*

2. **`projk` is RECOMPUTABLE from the persisted K store.** The router index is just the frozen ±1 Rademacher projection of each position's post-RoPE K (`sp_arm_project`). The Ring-2 K stream (the stdio backend's `sp_arm_ring2_k.bin`) already persists that K. So episode persistence does **not** need to serialize `projk` — it can be rebuilt by re-projecting the stored K on load. Persisted episode = {K store, V store, manifest} + a re-projection pass.

3. **Eviction is DOUBLY entangled, and the stdio store TRUNCATES on open.** (a) Dropping a block from the Ring-2 store alone does not change recall — the router selects by `projk` score over all positions independently; a curator that evicts must prune *both* the store and the `projk` index (or the rebuilt-from-store index, which makes #2 the clean path). (b) `sp_arm_ring2_stdio_open` uses `fopen(..., "w+b")` — "fresh store per open" — so the current decode **cannot load a pre-curated store**; replaying a curated episode needs a non-truncating load path or a registered backend that serves curated data.

**Net:** C1-lite's enabling capability is an **episode replay decode mode** (load persisted K/V + rebuild `projk` + score a window against it). The curator + gate compose on top of that. The cleverness (which heuristic) is downstream of the plumbing (can we replay a foreign episode at all).

## 2. Staged plan (each stage its own gate; null control first, per P1 discipline)

| Stage | What | Gate (its own metric) |
|---|---|---|
| **C1L.0 — episode persistence + replay** | A decode mode that LOADS a persisted episode ({`sp_arm_ring2_k/v.bin`, manifest}), rebuilds `projk` by re-projecting the stored K, and runs `qwen3_ppl_decode`-style scoring of a query window against it. Either a non-truncating stdio "load" backend or a registered in-RAM replay backend. | **G-C1L-0a (re-projection determinism): PASS 2026-06-09** — `tools/curator/curator_replay.c` (links the REAL `core/arm/arm.c`+`arm_scan.c`, no decode/model): synth episode → persist K + manifest → reload read-only → re-project with `sp_arm_project` → router index `projk` **bit-identical** to the original (store round-trip + frozen-R-from-seed + projk all bit-exact). Proves the replay foundation: a persisted episode's router index needs no serialization — it is recovered by re-projecting the stored K. **G-C1L-0b Step 1 (load seam + replay-recall null): PASS 2026-06-09** — added a non-truncating read-only load path `sp_arm_ring2_stdio_open_ro` (`fopen(...,"rb")`, refactored out of the existing `w+b` open via a shared `stdio_open_mode` helper; `core/arm/arm.c` + decl in `include/sp/arm.h`). Standalone driver (`src/replay_ro_test.c`, links the REAL `arm.c`+`arm_scan.c`): write a 24-block K+V episode via the spill open → reopen `_ro` → **truncation-guard PASS** (k/v.bin both 768 B intact, vs the `w+b` contrast which zeroes them) → 24/24 blocks read back **bit-identical** → re-project reloaded K → `projk` **bit-identical** to inline → `sp_arm_select` on reloaded K reproduces the exact inline recall set `{0,9,8,7,10,11}`. The load seam + recall-layer replay null are proven decoupled from the model. **G-C1L-0b Step 2 (decode-layer replay null): PASS 2026-06-09** (see §3a) — `SP_REPLAY` wired into `generate_kv_impl`; in-tree `T_GENKV_REPLAY_NULL` gate green on the canonical MinGW build (34/34): intact replay == baseline top-1 (bit-exact), zeroed episode diverges, replay-off floor bit-exact. **C1L.0b CLOSED** — episode persist + replay (load seam + recall layer + model-attention layer) all proven; the curator loop (C1L.1 green) + cold-evict (C1L.2) compose on this. |
| **C1L.1 — the loop, identity curator** | propose→gate→promote/rewind harness with a NO-OP curator (proposes the episode unchanged to a shadow dir). | **G-C1L-1 (loop null): PASS 2026-06-09** — `tools/curator/curator_core.c` (standalone, WSL gcc, synthetic episode): G-C1L-1a identity→gate_delta=0→promote→canonical byte-identical + size unchanged; G-C1L-1b forced-bad-shadow→gate_delta>0→rewind→canonical untouched + shadow gone; both receipted (append-only `receipts.log`). Cold-evict preview shrinks 32790→24598 B (structural; PPL gate is C1L.0). The transactional immune system proven in isolation, decoupled from replay. |
| **C1L.2 — cold-evict heuristic** | instrument `sp_arm_select` to count per-position recall hits (the access/association signal — RFC §7 = the LRU telemetry); curator evicts the coldest K% of positions from {K store, V store} and rebuilds `projk` over survivors. | **G-C1L-2 Step 1 (recall-hit telemetry): PASS 2026-06-09** — `sp_arm_hits_attach/detach` + per-position top-k counter in `sp_arm_select{,_sig}` (sinks + window excluded as structural; `core/arm/arm.c` + `arm.h`). Gate `T_GENKV_RECALL_HITS` (canonical MinGW, 39/39): **telemetry-null** — attaching the counter leaves the decoded sequence bit-identical; **signal** — non-uniform histogram `0 0 25 36 36 27 28 16 14 23 24 8 7 9 15 7 10 3 4 3 4 4 1 0 0 0 0 0` (total 304, 21 hot / 7 cold), a clean content-recall gradient with identifiable cold positions. **G-C1L-2 Step 2 (cold-evict consolidation): PASS 2026-06-09** — `sp_arm_evict_attach` mask in `sp_arm_select` (f32 router; a masked position is skipped as a recall candidate; NULL = bit-exact). Gate `T_GENKV_COLD_EVICT` (canonical MinGW, **45/45**): **COLD evict** of all 7 zero-hit positions → decode **bit-identical** (a zero-hit position never won top-k, so it cannot leave any recall set → lossless) ⇒ curator **PROMOTES** the shrunk episode; **HOT evict** of the single most-recalled position (pos 3, 36 hits) → decode **DIVERGES** ⇒ curator **REWINDS** (too-aggressive evict caught). The full consolidation loop — propose → gate by sequence identity → promote/rewind — proven on real recall. **C1L.2 CLOSED. C1-lite COMPLETE** (C1L.0a re-projection + C1L.0b replay + C1L.1 transaction + C1L.2 cold-evict all green). Scope: proven on the qwen3 CPU two-ring (uniform geometry); the gemma4 port is P3 (§3b: G-P3-GEOM + G-P3-SHARED). |
| C1L.3 (stretch) | merge-adjacent-similar instead of pure evict (Spinor-block average of near-duplicate positions) | recall@budget ≥ cold-evict at equal episode size |

## 3. Gate currency & receipts

PPL via `qwen3_ppl_decode` (teacher-forced, the C2.1 instrument). Episode size = stored position count × per-position bytes. Every promotion logs a receipt: positions evicted/merged, before/after PPL + size, accept/reject reason — the auditable-dream record (RFC §6). Model: the proven `qwen3_rt.sp-model` (OK_Q8 swivel). Honest bound carried from C2: Spinor-KV recall is a ~6.5%-argmax-flip lossy overlay, so the replay null gate is bounded-divergence, not bit-exact (the weight path + recall-off remains the bit-exact floor).

## 3a. C1L.0b Step 2 — the decode.c replay surgery (landed 2026-06-09, gate run pending on the canonical build)

The non-truncating `_ro` seam is now wired into the canonical decode (`core/forward/decode.c`, `generate_kv_impl` — shared by `qwen3_generate_kv` and `qwen3_ppl_decode`). Five **gated, off-path-inert** edits:

1. Globals `g_replay` / `g_replay_dir` / `g_replay_npos` (default 0/"."/0).
2. Env reads `SP_REPLAY` / `SP_REPLAY_DIR` / `SP_REPLAY_NPOS` in `read_env_knobs`.
3. A `replay_be` backend opened via `sp_arm_ring2_stdio_open_ro` just before the position loop — **admissible only on the plain f32 path** (`!use_blocks && !fusion_on`); inadmissible configs leave `replay_on=0`.
4. The injection: immediately after the per-head RoPE and **before** the projk build, for `pos < g_replay_npos` the computed post-RoPE `knew`/`vnew` are overwritten by episode block `(L,pos)` at byte `((L*P)+pos)*KVD*sizeof(float)` (the ring2 stdio spill layout). Everything downstream — `projk`, the Ring-1/2 cache, the spill, attention — then consumes the loaded history. The live query `q` is **not** replaced.
5. `done:` closes the owned `replay_be`.

**Off-path floor:** `SP_REPLAY` unset → the branch is never entered, zero allocations, byte-for-byte the prior canonical forward (the bit-exact replay-off guarantee). The Linux sandbox compiler parsed through all five edit sites without error (the only failures were the workspace mount truncating decode.c's tail + a stale-arm.h artifact — tooling, not code; see [[feedback-workspace-mount-stale-partial]]). The math-core CPU path is **Windows MinGW (build/)** + the real `qwen3_rt.sp-model`, so the gate runs there, not in the sandbox.

**G-C1L-0b Step 2 (decode-layer replay null): PASS 2026-06-09** — in-tree gate `T_GENKV_REPLAY_NULL` added to `core/session/arm_genkv_gate.c` (the T_ARM_GENKV harness; synthetic qwen3 fixture, HD=8, decodes through the canonical `qwen3_generate_kv`). Built on the canonical Windows MinGW (gcc 15.2, ninja; libsp_arm.a + libsp_forward.a relinked) — **34/34 checks, 0 fails**. CAPTURE spills the full 28-position episode to `epcap/` (== baseline). REPLAY-INTACT (`SP_REPLAY=1 SP_REPLAY_DIR=epcap SP_REPLAY_NPOS=28`, ring/recall off) overwrites every position's post-RoPE K/V from the `_ro`-loaded episode → **top-1 sequence byte-identical to baseline** (the loaded K/V flow losslessly through projk + the f32 cache + attention; stronger than the PPL-within-Spinor-bound the gate asked for — the stdio store is f32-exact so replay is bit-exact). NEGATIVE control (zeroed `epcap/*.bin`) **DIVERGES** (`…34 13 41 34 13 41…` degenerate loop vs baseline `…34 34 13 32 34 19 13…`), proving the loaded bytes drive attention. The other 10 gates run `SP_REPLAY=0` → the canonical forward is **bit-exact (replay-off floor confirmed)**. Recall-layer (Step 1) + load seam + model-attention layer (Step 2) all green ⇒ **C1L.0b CLOSED**; episode replay is the enabling capability for the curator loop (C1L.1 already green) + cold-evict (C1L.2).

Original turnkey procedure (now executed; retained for the real-`qwen3_rt.sp-model` reproduction):

- **CAPTURE** (writes the episode + records PPL_A): `SP_RECALL_B=<B> SP_RECALL_W=<W> SP_RING2=1 SP_RING2_DISK=1 SP_RING2_DIR=ep` → `ep/{sp_arm_ring2_k.bin,sp_arm_ring2_v.bin}` persisted, PPL_A reported.
- **REPLAY (intact)**: same recall/ring2 config (spilling to a fresh `ep2`) **plus** `SP_REPLAY=1 SP_REPLAY_DIR=ep SP_REPLAY_NPOS=<P>` → historical K/V overwritten by the loaded (bit-identical) episode → **expect PPL_replay == PPL_A bit-identical** (the f32 stdio store is lossless; the Spinor bound only applies to a Spinor-coded episode). This proves the load+inject seam routes disk bytes faithfully into the live K/V attention consumes — without corruption.
- **NEGATIVE CONTROL (zeroed episode)**: `dd if=/dev/zero` over `ep/*.bin` (or a curator-perturbed copy), replay again → **expect PPL materially worse** → proves the loaded bytes actually drive attention (replay is not a silent no-op).
- **CONTROL (replay-off)**: drop `SP_REPLAY` → PPL byte-identical to the no-replay baseline (the floor).

Intact-faithful + zeroed-divergent + off-identical is the honest decode-layer analog of the Step-1 standalone null already passed. Recall-layer + load-seam already green (§C1L.0b Step 1); this closes the model-attention layer.

## 3b. P3 PRE-FLIGHT — gemma4 geometry compatibility audit (2026-06-09, grounded in `core/forward/gemma4.c` + `include/sp/model.h`)

**Why this section exists (operator challenge, 2026-06-09):** XBAR's deployment target is gemma-4-12B, but the ARM two-ring + curator + replay are wired ONLY into the qwen3 CPU decode (`core/forward/decode.c`; `core/forward/gemma4.c` is forward-only — no decode, no KV cache, no ring). qwen3 is therefore the correct substrate for proving the **model-agnostic curator ALGORITHM** (transactional loop, episode replay) cheaply and bit-exactly — but a qwen3-green gate does **not** prove gemma4-readiness. This audit reads the real gemma4 geometry and scopes exactly what the P3 port must handle. It already **corrected one false alarm and surfaced the real gaps**.

gemma4 geometry ground truth (line-cited): per-layer class `global = (L % g4_swa_period==period-1)`, period 6 → 8 global / 40 SWA over 48 layers (`gemma4.c:148`). GLOBAL = n_head 4 / **n_kv 1 / head_dim 512**, RoPE 1e6 + proportional freqs, full attention (`gemma4.c:11,70,148-157`). SWA = **n_kv 2 / head_dim 256**, RoPE 1e4, sliding window (`gemma4.c:14,71`). Shared-KV: layers `[0,g4_n_kv_from_start)` own K/V; trailing layers reuse an owner (shared SWA→kvfs-2, shared global→kvfs-1) and **skip their own K/V projection** (`gemma4.c:20-22,174,190`; `model.h:52`).

| # | Curator structure | gemma4 reality | Verdict |
|---|---|---|---|
| 1 | episode block layout `((L*P)+pos)*KVD*4` (single KVD) | **KVD is CONSTANT 512** both classes (SWA 2×256, global 1×512; `gemma4.c:16,79`) | **COMPATIBLE** — the false alarm. The byte layout + the decode.c replay injection survive gemma4 unchanged dimensionally. |
| 2 | recall router `projk[L][P][NKV][r]` + `sp_arm_select` project per-kv-head over `head_dim` | per-layer-class **NKV (1 vs 2) and head_dim (512 vs 256)** | **GAP — P3.** projk + select must carry per-layer-class NKV/HD; a single global NKV/HD (qwen3) is wrong for gemma4. |
| 3 | ring spills EVERY layer's K/V per position; recall reads `(L,pos)` | shared layers (`L≥kvfs`) **mint no K/V**; reuse an owner's | **GAP — P3.** curator must skip shared layers on spill and **owner-indirect** on recall; episode manifest needs an owner map / own-vs-shared skiplist. |
| 4 | one uniform Ring-1 window/sink policy | SWA layers carry the model's OWN sliding window; globals are full-attn | **GAP — P3.** ring-window × model-SWA interaction is untested; globals (full-attn, 1 kv-head, 75% unroped) and SWA need separate recall policy. |
| 5 | episode stores K and V as **independent** blocks | **RESOLVED 2026-06-09:** the SP gemma4 path (oracle + CUDA, gated KL 2.66e-10) projects V via `attn_v` + weightless RMSNorm for **both** classes — no `V=K` aliasing (`gemma4.c:18,50,179`). The eta-5b "V-less" line was a read of *llama.cpp's* reference, not SP's mint. | **COMPATIBLE — not a curator blocker.** Independent K/V episode blocks are correct for the SP gemma4 decode. (Separate lane: whether SP's globals *should* be V-less vs the real model is a gemma4-forward question, not C1-lite's.) |

**P3 sub-gates (named now so the port has explicit targets) — TWO real, after the V-less item resolved:** G-P3-GEOM (per-layer-class NKV/HD in projk + select; parity vs gemma4 oracle), G-P3-SHARED (shared-KV skip/owner-indirect; episode owner map; replay null on a shared-layer episode). Lower-risk follow-on: G-P3-WIN (SWA-window × ring-window; globals full-attn recall). ~~G-P3-VLESS~~ RESOLVED (finding #5; SP projects V independently → no episode-format change). The decode.c replay seam (`SP_REPLAY`) and the episode byte layout transfer **as-is** (finding #1); the router + shared-KV handling are the real port work.

> **G-P3-GEOM SUBSTRATE LANDED — 2026-06-10, core `64b698c`.** Additive geom API in core/arm (`sp_arm_geom` per-layer {nkv,hd,off} descriptor + `sp_arm_geom_layout` sizing + `sp_arm_project{,_sig}_geom` / `sp_arm_select{,_sig}_geom` with hd_max-strided Rademacher reads); the legacy uniform entry points now DELEGATE to the geom bodies (one code path — drift impossible). Gate **T_ARM_GEOM 26/26**: per-class re-projection determinism (the C1L.0a property per-class), select vs brute-force f32 oracle, **uniform-null** (geom == legacy bit-exact), and a stride-misread guard (hd-as-stride on a 256-class measurably fails — the audit's corruption mode is now gated). Prior gates re-green: T_ARM 48/48, T_ARM_GENKV 45/45, core suite 23/23 incl. M_QWEN36. L1 ABI + episode byte layout untouched; qwen3 decode deliberately NOT rewired. **Remaining for P3 proper:** decode.c geom wiring (`sp_arm_geom_layout` replaces the uniform projk malloc @ decode.c:317; select sites → `_geom`) + G-P3-SHARED owner-indirection. Same-day hardening: gemma4.c shared-KV owner bounds guard (core `c608b2f`).

**Scope statement (no false readiness):** C1L.* green = the curator's transactional control flow + replay/persistence MECHANISM proven on uniform-geometry KV. It does NOT certify the gemma4 jagged/shared/V-less cache — that is P3, gated above. Development stays on the fast bit-exact qwen3 substrate (operator-endorsed); P3 is the gemma4-readiness gate.

## 4. Scope discipline

C1-lite proves the curator's **control flow + a working heuristic on real KV**, nothing about learned compression (that is C2 = the P2.b adapter applied to ring state). It runs entirely on the qwen3 CPU path; the gemma4-CUDA ring is P3. NIGHTSHIFT (N1) = C1-lite's loop run offline under schtasks over a persisted episode — so C1L.0's persistence format is also NIGHTSHIFT's episode format. No silent gate revisions; surface upstream.
