---
type: memory
title: B4 exact IO-aware KV tiling REDUNDANT: prefill attention alr
description: B4 exact IO-aware KV tiling REDUNDANT: prefill attention already fused/on-chip (scores in shared mem); bottleneck is weight-dequant not attention; per-token prefill = no query batch to tile; SWA fits L2. PARKED.
timestamp: 2026-07-08T19:35:13Z
resource: TBD
sp_status: HONEST-NEGATIVE
sp_gate: G-B4-FEASIBILITY
sp_commit: TBD
sp_repro: none
mem_kind: agent
mem_addr: 356be46556a1953e
mem_verified: unverified
mem_lifecycle: active
tags: [b4, kv-tiling, flashattention, prefill-attention, io-aware, weight-dequant, bottleneck, honest-negative, G-B4-FEASIBILITY, agent, tier-2]
mem_tier: full
---

B4 exact IO-aware KV tiling (arXiv 2604.21215 Recurrent-Transformer / FlashAttention-style) is REDUNDANT for the Shannon-Prime engine (G-B4-FEASIBILITY, read-only recon). Prefill attention ALREADY fuses QK->softmax->.V with the score row in SHARED memory: batched cold k_attn (cuda_forward.cu:968-1002, launch :4960) and the production per-token path (k_attn_decode_ring[_bx] :670/:716 SWA, k_attn_decode_win[_bx]/_dyn :556/:2346 globals). No kernel materializes an [NxN] score matrix to HBM and there is no separate softmax kernel, so FlashAttention's HBM-traffic win is PRE-CAPTURED. Bottleneck is the WEIGHT-GEMM dequant (gemm_w_lift -> k_dequant_arena_q4b, cuda_forward.cu:1409-1415 = the ~98s cold n=1765 prefill cost center), NOT attention (~1.05TB attn KV upper bound vs ~11-85TB weight dequant at N=1765). Production prefill is PER-TOKEN (single-query) so there is no query batch to tile -> FlashAttention query-tiling is structurally inapplicable to the shipped path. 8 global layers (hd=512, O(N^2)) ~20% of attn traffic; 40 SWA (hd=256, window ~1024, O(N*w)) ~80% and the 2MB SWA window fits the 2060's 3MB L2 (already L2-served). Byte-exact prefill exists only via the per-token path (k_attn_decode_win_bx/ring_bx, Garner dot + FB30 integer softmax); the batched k_attn is float-only (bit-parity NOT claimed). Effort if built anyway = multi-week (2 head shapes x 2 exactness modes incl. a new streaming-integer-softmax gate). Redirect prefill-perf to the dp4a weight-dequant lane (SP_KV_PREFILL_DP4A already exists). PARKED.
