---
type: memory
title: N5c-v3 resident DevTensor cache, DENSE-backbone first cut (e
description: N5c-v3 resident DevTensor cache, DENSE-backbone first cut (engine 160a9e5): mechanism GREEN - 206 dense tensors resident, byte-identical, rc=0, no OOM, VRAM-budget guard (9.0->6.7GB). Dense-only win SMALL at prefill (~1.2s/28s ~4%, dense backbone only ~2.4GB) but ~35% at decode (4.37->2.78s @n_tok=14). Localizes the real PCIe tax to the EXPERTS (~12GB, ~all 128 fire at prefill - confirmed trace cnt 322/638/703). Gemini 'upload dead unselected experts' premise WRONG: dispatch already skips cnt==0 (line 6440); tax is RE-uploading SELECTED experts every step. NEXT = expert LRU (extend same cache to dg_gemm_packed_rows, capacity-bound) = where the real prefill win lives. Gated SP_DG_WCACHE default-off=null floor.
timestamp: 2026-06-22T01:22:36Z
resource: TBD
sp_status: GREEN
sp_gate: none
sp_commit: TBD
sp_repro: none
mem_kind: agent
mem_addr: 1d899e7c932f543e
tags: [n5c-v3, resident devtensor cache, dense backbone, SP_DG_WCACHE, no eviction, budget-stop cudaMemGetInfo, 206 residents, mechanism green, dense win small, experts are the tax, expert LRU next, 160a9e5, gemini premise wrong cnt==0 skip, agent, tier-2]
mem_tier: full
---

ï»¿# N5c-v3: resident DevTensor cache â€” dense backbone first cut (mechanism GREEN, win localizes to experts)

Engine commit 160a9e5 (lineage 9cc65e2 -> 160a9e5).

## What
Keyed-by-name device DevTensor residence cache in dg_gemm_packed (dense/attn/head): on hit, reuse
the resident DevTensor + SKIP upload_packed -> kills the per-forward packed-weight PCIe H2D across
the diffusion judge's ~48 static-prompt steps. dg_wcache_lookup/insert/free + cudaMemGetInfo
budget-stop guard (never OOMs; stops caching near the floor, SP_DG_WCACHE_MARGIN_MB default 1536).
Gated SP_DG_WCACHE (default-off = byte-identical null floor). NO eviction (first cut). Experts
(dg_gemm_packed_rows) NOT cached this pass. Freed at release alongside dg_reservoir_free.

## Gate (test_dg_forward_timing, SM pinned 2100, fresh process/leg, NFWD=3)
Cache ENGAGES: 206 dense tensors resident (attn q/k/v/o + ffn gate/up/down x30 layers + token_embd),
free VRAM 9023 -> 6653 MB (~2.4 GB), rc=0, no OOM. Read cold-vs-warm INTERNALLY on the cache legs
(SP_DG_TRACE on both -> trace overhead cancels):
  n_tok=1852: off flat 28.8/27.2/26.4 ; on 29.2(cold) -> 28.0(warm) = ~1.2s saved (~4%)
  n_tok=14:   on 4.37(cold) -> 2.78(warm) = ~1.6s saved (~35%)

## Verdict (honest)
Mechanism PROVEN (residence works, byte-identical, safe, VRAM-budgeted). Dense-only SPEED win is
SMALL at prefill (~1.2s/28s) because the dense backbone is only ~2.4GB -> its re-upload is a sliver
of the prefill wall. LARGER at decode (~35%) where the dense share is bigger. This LOCALIZES the
real PCIe tax to the EXPERTS (~12GB selected each forward at prefill; coupon-collector saturation
confirmed: trace shows expert cnt 322/638/703 at n_tok=1852, ~all 128 fire). Gemini's "we upload
dead UNSELECTED experts" premise is WRONG: dg dispatch already skips cnt==0 (cuda_forward.cu:6440);
the tax is RE-uploading the SELECTED experts every step, which an expert residence cache eliminates.

## NEXT
Expert LRU: extend the same keyed cache to dg_gemm_packed_rows (expert slices), capacity-bound by
the budget guard -> at prefill ~all 128 fire with high cross-step overlap, so ~(resident fraction)
of the ~12GB expert H2D is eliminated on steps 2..48. THIS is where the real prefill win lives.
Receipt: tests/fixtures/chat_fullstack/G-DG-N5c-WCACHE-dense.log.
