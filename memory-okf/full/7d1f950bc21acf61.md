---
type: memory
title: MTP CORRECTION: daemon spec_step verifies SEQUENTIALLY (no speedup); real gap = a BATCHED target-verify verb, not the draft source
description: spec.rs spec_step verifies one target decode_step per accepted token (slower than plain decode). Not the accelerating version (sp_toks.c used a batched qwen3_forward verify). Real MTP prerequisite = batched target-verify verb in the CUDA kvdecode path (K tokens->K logits, accept prefix, rewind). Draft source/VRAM come after. Don't wire the sequential spec_step (non-functional). Correct CONTRACT-C4-SPECDECODE.
timestamp: 2026-06-28T07:34:51Z
resource: 0bcaeff
sp_status: DESIGN
sp_gate: MTP-corrected
sp_commit: 0bcaeff
sp_repro: none
mem_kind: agent
mem_addr: 7d1f950bc21acf61
tags: [MTP correction, spec_step sequential verify no speedup, batched verify is the real gap, not draft source, CONTRACT-C4-SPECDECODE correction, kvdecode batched verify, do not wire sequential spec_step, agent, tier-2]
mem_tier: full
---

ï»¿CRITICAL MTP CORRECTION (read spec.rs 2026-06-28): the daemon spec_step (sp_daemon/src/spec.rs) VERIFIES SEQUENTIALLY -- Phase 2 does one target.decode_step PER accepted draft token (lines 56-60). So it costs the SAME target forwards as plain decode PLUS the draft overhead = it is NOT a speedup, it is a SLOWDOWN. It is a correctness scaffold only. The sp_toks.c '2.67x fewer forwards' used a BATCHED qwen3_forward verify (one forward verifies K), which the daemon's single-token kvdecode path does NOT have. THEREFORE the real MTP blocker is NOT 'a high-acceptance draft source' (as CONTRACT-C4-SPECDECODE framed) -- it is a BATCHED TARGET-VERIFY verb in the CUDA kvdecode path (feed K draft tokens in one forward -> K logit-vectors -> accept prefix -> rewind rejected). That batched-verify (reuse the batched prefill kernel) is the prerequisite; the draft source + VRAM come after. Wiring the current sequential spec_step would be a non-functional stream. CORRECT the C4 plan: increment 0 = batched target-verify verb; then draft source; then wire. spec_step stays as the accept/rewind reference.
