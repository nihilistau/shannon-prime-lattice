---
type: memory
title: MTP scope: e2b draft does NOT fit VRAM on 2060 -> first draft = prompt-lookup (VRAM-free, matches proven sp_toks.c); G-SPEC-WIRE = wire it into routes.rs
description: spec_step proven+unwired. gemma4-e2b draft (4.65GB)+12B(9.4GB)>12GB 2060 -> won't fit. First draft = prompt-lookup (n-gram, VRAM-free, proven in sp_toks.c SP_MTP: 2.67x fewer forwards, 0.87x real). Needs a prompt-lookup token-proposer variant of spec_step (it drives a session today) OR a ~1.2GB Q4B e2b draft. G-SPEC-WIRE = port into routes.rs decode loop, default-off, gate null-floor+accept-length. branch feat/spec-wire.
timestamp: 2026-06-28T07:28:08Z
resource: 0bcaeff
sp_status: DESIGN
sp_gate: G-SPEC-WIRE (planned)
sp_commit: 0bcaeff
sp_repro: none
mem_kind: agent
mem_addr: 42752d41b46bb74f
tags: [MTP campaign scope, spec_step unwired, e2b draft does not fit VRAM, prompt-lookup first draft, sp_toks.c SP_MTP, G-SPEC-WIRE, token-proposer variant, 2060 12GB, feat/spec-wire, agent, tier-2]
mem_tier: full
---

ï»¿MTP campaign scoping (2026-06-28). spec_step (sp_daemon/src/spec.rs) is PROVEN + UNWIRED (called nowhere outside spec.rs; routes.rs /v1/chat decode loop at ~516 is single-token child.decode_step). CONSTRAINT: the natural same-vocab draft gemma4-e2b (4.65GB) does NOT fit VRAM alongside the 12B (9.4GB) on the 12GB RTX2060 (~14GB > 12GB; card already ~11.9GB w/ 12B+judge). So the FIRST draft source must be VRAM-FREE = PROMPT-LOOKUP (n-gram), matching the proven sp_toks.c SP_MTP loop (2.67x fewer forwards, mean-accept 1.78/8, 0.87x on real prose). spec_step drives a draft SESSION -> G-SPEC-WIRE needs a prompt-lookup token-proposer variant of spec_step (no session/VRAM), OR a Q4B-quantized small same-vocab draft (~1.2GB e2b-Q4B) that fits. G-SPEC-WIRE = port the prompt-lookup spec loop into the routes.rs decode loop (substantial: per-accepted-token streaming/stop/EOS handling; default-off SP_SPEC = null floor), gate null-floor bit-identical + accept-length. Branch feat/spec-wire created. REAL win G-SPEC-ACCEPT = high-acceptance draft (DSpark hybrid head, or a fitting Q4B draft). prompt-lookup is C-only today (sp_toks.c); port to Rust. Then #2 Ring-2 streaming.
