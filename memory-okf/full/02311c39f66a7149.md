---
type: memory
title: Telemetry flywheel GREEN: every recall decision logged, class-redacted (G-LM-TELEMETRY, LM-B2)
description: ADR-005 LM-B2 GREEN (engine decision-telemetry core). routes.rs telem_emit_recall logs a structured JSONL record at all 3 terminal recall branches (decline/margin-skip/deliver): query/entry/class/cos/margin/delivery/decision. Private-secret queries REDACTED to a hash (privacy-before-data). Gated SP_TELEMETRY=1 + SP_TELEMETRY_LOG, off token path, default-off null floor. GATE: mixed corpus -> 6 records (private-secret 4, counterfact 1 systemecho, persona 1 system), cos[deliver] 0.985 vs cos[decline] 0.932, REDACTION PASS (secret 0 hits in log). This IS the data to tune tau/margins, build a finetune set, detect drift. DO NOT rebuild. Follow-on: per-turn timings + OUTPUT-join (engine emits telemetry SSE event on events_tx, harness StreamProcessor sinks with output+obey/leak) + PoUW SpinorReceipt per record. Next: LM-B3 adaptive classify. telemetry_report.py = the view.
timestamp: 2026-07-03T12:28:14Z
resource: engine 6431e55
sp_status: GREEN
sp_gate: G-LM-TELEMETRY
sp_commit: engine 6431e55
sp_repro: run_classify.bat gate (SP_TELEMETRY=1) + classify_run.py gate + telemetry_report.py <log> <secret>
mem_kind: agent
mem_addr: 02311c39f66a7149
mem_class: fact
mem_delivery: recite
mem_authority: supplements
mem_confidence: 0.9
tags: [telemetry, flywheel, SP_TELEMETRY, telem_emit_recall, redaction, privacy, adr-005, lm-b2, tuning-data, finetune, agent, tier-2]
mem_tier: full
---

ï»¿G-LM-TELEMETRY (LM-B2) â€” the telemetry flywheel: every recall DECISION logged as a structured,
class-REDACTED telemetry-okf record. The data to tune/finetune/detect-drift. (2026-07-03)
=========================================================================================
Second brick of ADR-005 (CONTRACT-LIVING-MEMORY LM-B2). The engine now records what the memory
system DECIDED each turn â€” off the token path, privacy-first.

BUILD (engine this-commit, gated SP_TELEMETRY=1 + SP_TELEMETRY_LOG=<path>, default-off null floor):
  * routes.rs telem_emit_recall(query, entry, class, cos, margin, delivery, decision): appends one
    JSONL record at the recall DECISION point. Called at all three terminal branches â€” decline
    (attr-gate zero-inference), margin-skip, deliver. REDACTION (ADR-005 Â§3b): when the fired
    entry's class == private-secret, the query is replaced by a hash (#<hash>) and redacted:true â€”
    telemetry can NEVER carry a value the delivery path would have declined.
  * telemetry_report.py: the tuning/drift view â€” per-class/decision/delivery counts, cos-by-decision,
    margin stats, redaction check.
  * The engine logs the DECISION + (follow-on) timings; the harness joins OUTPUT + outcome by turn
    (it already has both) â€” one stream, two sides (ADR-005 Â§3).

GATE G-LM-TELEMETRY â€” served the mixed classify corpus (2 private-secrets, 1 counterfact, 1
persona) with SP_TELEMETRY=1; ran the query set. 6 structured records produced:
  by class:    private-secret 4 Â· counterfact 1 Â· persona 1
  by decision: deliver 4 Â· decline 2
  by delivery: recite 2 Â· attr-gate-strict 2 Â· systemecho 1 Â· system 1
  cos[deliver] mean 0.985 (0.950â€“1.000) Â· cos[decline] mean 0.932 Â· margin mean 0.049
  redacted records: 4/4 private-secret (query = hash)
  REDACTION CHECK: secret "K9X-4M2-8QP" appears in the log 0 times -> PASS.
Each private-secret query is a hash (#3259e7fdâ€¦, #0554cbe8â€¦); counterfact/persona queries are in
the clear. Every decision â€” which entry, class, cos, margin, delivery mode, deliver/decline â€” is
now queryable data.

VERDICT: GREEN (engine decision-telemetry core). The flywheel produces a structured, class-
redacted per-decision stream: the raw material to (1) tune Ï„/margin/class-defaults from real
distributions, (2) build a finetune set (queryâ†’delivery/class), (3) detect drift over time.
Privacy-before-data proven (0 secret hits). Default-off = byte-identical.

SPEED: one JSONL append at the decision point, off the token path. Sub-ms. No forward added.

FOLLOW-ONS (CONTRACT LM-B2 remainder): add per-turn timings (prefill_s/decode_s/tok_s) + the OUTPUT
join (engine emits a `telemetry` SSE event on events_tx; harness StreamProcessor sinks it with the
answer + obey/leak outcome); mint a PoUW SpinorReceipt per record (addr = join key). Then LM-B3.

REPRO: run_classify.bat gate (SP_TELEMETRY=1); classify_run.py gate; python telemetry_report.py
_classify_corpus/telemetry.jsonl K9X-4M2-8QP. ENV: engine this-commit; RTX 2060 12GB.
