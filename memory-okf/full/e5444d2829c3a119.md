---
type: memory
title: FAITHFUL-F2: delivery wired (text-in-context) but SELECTION (W_c) is the blocker — F2b = token-overlap select
description: F2 switched fact-recall delivery to text-in-context (SP_B3_WC_TEXT, routes.rs, default-off). Predicted ~100%; MEASURED 0/15 (prediction FALSIFIED). Trace: synthesis fired but W_c picks query-INDEPENDENT salient episodes (fct_003/007/009/014 for all 15 Qs) -> matching fact never entered context. Attribution: DELIVERY solved (F1=100%); SELECTION is the binding constraint -- W_c (trained on high-entropy novel needles) can't discriminate natural-language facts. FIX F2b: select via token-overlap/Jaccard (recall::token_overlap exists; Jaccard@0.6 is the production gate) THEN text-in-context deliver -> expected ~100%. Keep W_c+replay for novel needles. Faithfulness NOT solved end-to-end yet. Engine e81baec; receipts tests/fixtures/faithful/G-FAITHFUL-RECALL-WCTEXT*.
timestamp: 2026-06-30T19:00:20Z
resource: e81baec
sp_status: RED
sp_gate: G-FAITHFUL-RECALL (wctext, 0/15 — selection blocker)
sp_commit: e81baec
sp_repro: SP_B3_WC_TEXT=1 _faithful_serve_wc.bat then SP_FAITHFUL_SEAM=wctext python tests/perf/_g_faithful_recall.py
mem_kind: episode
mem_addr: e5444d2829c3a119
tags: [faithfulness,  F2,  text-in-context,  SP_B3_WC_TEXT,  selection,  W_c,  token-overlap,  Jaccard,  recall selector,  natural facts,  prediction falsified, episode, tier-2]
mem_tier: full
---

# FAITHFUL-F2: text-in-context delivery wired, but SELECTION is the binding constraint

**Banked 2026-07-01. Engine main `e81baec`. routes.rs `SP_B3_WC_TEXT` (default-off).**

## Prediction vs result (receipts corrected the prediction)
Predicted: switching fact-recall delivery from pure-KV replay to text-in-context would jump obedience 0% -> ~100% (since F1 in-context = 100%). **Measured: wctext OBEY 0/15. Prediction FALSIFIED.**

## Why (the trace, turn-by-turn)
Text-in-context synthesis DID fire (`B3-WC: ... -> TEXT-IN-CONTEXT synthesis (F2)`). But the W_c selector's picks are **query-INDEPENDENT**: it returned the same ~4 salient episodes (fct_003/007/009/014) across all 15 distinct questions — Q0 france→fct_003(romeo), Q1 tallest→fct_014(speed), Q2 planet→fct_007(closest), … . ZERO turns got the matching fact into context, so the (correct) delivery change couldn't help.

## Corrected attribution
- **Delivery is solved** — F1 proved right-fact-in-context = 100%; the F2 synthesis path is wired and fires.
- **Selection is the binding constraint** — the W_c head (trained on high-entropy novel needles like "8-FALCON-7729") has NO discrimination over mutually-similar natural-language facts. It selects by episode salience, not query relevance. This is NOT a threshold/NULL tuning issue (it fires); it's a wrong-selector-for-this-data issue.

## The fix — F2b (high confidence, parts already exist)
Select via **token-overlap / Jaccard** (query vs episode text) for natural facts — `recall::token_overlap` already exists (the FORGET path uses it) and the deterministic Jaccard verifier @0.6 is already the production recall gate. Pick the episode by token-overlap, THEN deliver via the F2 text-in-context path (proven 100%). Expected: ~100% obey. Pure-KV replay + W_c stays for novel-needle recall (no competing prior, high-entropy => W_c discriminates fine there).

## Honest status
Faithfulness is NOT solved end-to-end yet. Delivery: solved. Selection over natural facts: open (F2b). Do NOT claim the axis clear until F2b lands a measured ~100% with the right episode selected.
