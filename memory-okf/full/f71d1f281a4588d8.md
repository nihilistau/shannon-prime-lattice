---
type: memory
title: N5c MoE flip = 4.6x per-forward collapse (80.6s -> 17.5s f32->packed dp4a), rc=0 parity
description: MEASURED (test_dg_forward_timing warm, engine 16cfff2): diffusion forward 80.58s (all f32) -> 17.47s (dense-MLP + 128-expert MoE all packed dp4a via dg_gemm_packed_rows) = 4.6x / -78%, rc=0 (int8 act-quant ~1e-3 parity, same as dense decode). The MoE was the bulk of the 26B = the wall. Packed OK_Q4B across PCIe (half the f32 H2D) + dp4a unpack on-device; no new kernel, gated SP_DG_PACKED (default-off=f32 null floor). Still f32: Wq/Wk/Wv/Wo + head (more on the table). 48-step query ~64min -> ~14min. Prior: dense-only flip was -5.6% (b23c0f0); N5b host-reservoir was DEAD overhead (82s>78s). NEXT: flip attention+head, map sp_forward_dispatch_fn + Rust ABI streaming, VRAM hot-expert LRU, purge N5b.
timestamp: 2026-06-21T21:23:17Z
resource: engine 16cfff2
sp_status: GREEN
sp_gate: G-DG-N5c
sp_commit: engine 16cfff2
sp_repro: SP_DG_PACKED=1 build-cuda/tests/test_dg_forward_timing.exe; _n5c_moe.log: PACKED=0 80580ms vs PACKED=1 17466ms warm
mem_kind: agent
mem_addr: f71d1f281a4588d8
tags: [n5c, diffusion judge, MoE packed, dp4a, 4.6x speedup, 80s to 17s, dg_gemm_packed_rows, gemv_w_packed, PCIe collapse, SP_DG_PACKED, reuse not rebuild, parity rc0, agent, tier-2]
mem_tier: full
---

N5c MoE FLIP = THE LATENCY COLLAPSE (Phase-5 architecture proven on the metal).

MEASURED (test_dg_forward_timing, cudaEvent warm forward, CL=8 n_tok=14, engine 16cfff2):
  SP_DG_PACKED=0 (all f32):                fwd1 81130 ms, fwd2 80580 ms  -> ~80.6s
  SP_DG_PACKED=1 (dense-MLP + 128-expert MoE all packed dp4a): fwd1 19111 ms, fwd2 17466 ms  -> ~17.5s
  => 4.6x speedup / -78% per forward. rc=0 BOTH legs (no precision bled; int8 act-quant ~1e-3, identical to the dense dp4a decode path).

WHY: the 128-expert MoE is the bulk of the 26B-A4B weight. The f32 path (dg_upload_arena_rows) CPU-dequanted each hit expert slice to f32 + 28GB-equiv H2D every forward. The flip routes both per-expert GEMMs (gate_up + down) through dg_gemm_packed_rows -> upload_packed(14GB packed) + per-token gemv_w_packed (k_gemv_q4b_dp4a_v2, GPU dequant). Half the PCIe payload + dequant off the CPU critical path. NO new kernel - pure reuse of the dp4a path that keeps the dense 12B AR forward fast.

CAMPAIGN ARC (all receipts): N5b host-RAM reservoir = DEAD overhead (warm 82s > 78s, cache didn't hit; abandoned). dense-MLP flip = -5.6% proof (b23c0f0). MoE flip = -78% collapse (16cfff2). The architecture call (keep OK_Q4B compressed across PCIe, dp4a unpack on device) is proven end to end.

GATED: SP_DG_PACKED default-off = exact f32 path = null floor; f32 fallback on rc==2 (non-Q4B / in%32). Builds GREEN.

STILL F32 (more on the table): attention Wq/Wk/Wv/Wo + the LM head (the d19b140/b23c0f0 dg_gemm_packed handles dense weights; attention sites use the same pattern). 48-step query: ~64min -> ~14min now, less after attention+head.

NEXT: (1) flip Wq/Wk/Wv/Wo + head onto dg_gemm_packed. (2) map sp_forward_dispatch_fn (L1 6) + wire sp_daemon Rust ABI to stream the packed bytes (cpu_forward_dispatch.rs trampoline pattern). (3) VRAM hot-expert LRU (eliminate even the packed H2D for hot experts). (4) purge the dead N5b host-reservoir code. (5) re-run the full diffusion judge now that depth is affordable.
