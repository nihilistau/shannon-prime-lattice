---
type: memory
title: P3.4 recent-W union window RESTORES fluency under eviction (engine 4d7b904); router grading needs varied-filler harness
description: FIX: g4_kv_step global union reordered sinks->recent_W->pos->LSH (recent reserved before LSH, never clips); g4_arm_open derives arm_recent_w=min(512,Bslab/2). PROVEN fluency: ctx 2776>>Bslab 2048 eviction-active, needle in recent window -> FLUENT+correct 'The secret vault access code is 837492.' vs pre-fix '`'. The coherence-collapse fix WORKS. OPEN: far-needle (depth7) still '`' MISS but CONFOUNDED by repetitive synthetic filler (recent window = pure template repetition degenerates regardless of eviction). Router NOT cleanly graded. NEXT: varied-prose haystack -> isolate far-needle -> then validate/retrain LSH R.bin for gemma4-12b global-K. Scoreboard: VRAM-scaling GREEN (52K), fluency-under-eviction GREEN (recent_W), far-retrieval OPEN.
timestamp: 2026-06-30T13:24:08Z
resource: engine 4d7b904
sp_status: GREEN
sp_gate: fluency-under-eviction GREEN (depth95 fluent+correct); router OPEN
sp_commit: engine 4d7b904
sp_repro: _g_niah_daemon.py 256 1700 95 (HIT fluent) vs 7 ('`' confounded by filler)
mem_kind: episode
mem_addr: 1a3a3397eccae9f3
tags: [P3.4, recent-w, union, fluency, eviction, coherence, LSH, router, harness, OKV, 837492, episode, tier-2]
mem_tier: full
---

# P3.4 update: recent-W union window RESTORES fluency under eviction (engine 4d7b904); router grading still open

## The fix (committed, proven)
`g4_kv_step` global slab union reordered to **sinks → recent_W → pos → LSH-top-B** (recent reserved
*before* LSH, so it never clips). `g4_arm_open` derives `arm_recent_w = min(512, Bslab/2)` (env
`SP_ARM_RECENT_W` override; clamp `sinks+recent < Bslab`). 3 edits in cuda_forward.cu only — no FFI/daemon
change for the test (a `set_var`-style knob wouldn't reach C anyway; tunable cfg field = trivial follow-up).

## Proof — fluency restored
- ctx **2776 ≫ Bslab 2048** (eviction active), needle in the recent window (depth 95%): output now
  **FLUENT + correct** — `"The secret vault access code is 837492."` — vs the pre-fix degenerate `'```'`.
  The diagnosed coherence-collapse fix (global layers losing recent context) WORKS.

## Still open — far-needle / LSH router (honest)
- depth=7 (far needle, beyond recent_w → only LSH can page it): `'```'` MISS. BUT this is **confounded**:
  at depth=7 the recent 512 tokens are pure repetitive template filler ("warehouse shelf N holds M
  widgets"×N), which itself induces degeneration regardless of eviction (a non-evicted model would
  likely also degrade on 500 tokens of identical-template repetition). So the router is **NOT cleanly
  graded** — the degeneration is the harness filler, not a clean router miss.
- The discriminator is the recent-window CONTENT: depth-95 (needle+varied in recent) = fluent; depth-7
  (pure-repeat in recent) = degenerate. Same eviction config, different recent content.

## Next (clean router grading)
1. Rewrite the NIAH haystack with **varied prose** filler (not one repeated template) so the recent
   window is coherent regardless of needle position → isolates the far-needle on a fluent baseline.
2. Then a far-needle MISS is purely the LSH router → validate/retrain the `R.bin` router for the
   gemma4-12b global-K regime (cf B3-WC: relevance signals fail open-world until trained on the right signal).
3. G-OKV-P3-EVICT scoreboard: VRAM scaling GREEN (52K opens); fluency-under-eviction GREEN (recent_W);
   far-retrieval router = OPEN (needs varied-filler harness + router calibration).

Receipts: engine 4d7b904 (feat/okv-p3-global-evict); depth-95 HIT fluent / depth-7 '```' (confounded).
