---
type: memory
title: DIFFJUDGE PREFIX-KV REUSE = verified BYTE-EXACT ~40x lever (
description: DIFFJUDGE PREFIX-KV REUSE = verified BYTE-EXACT ~40x lever (LAUNCHPAD). PROOF (read tree, k_attn_diffusion ~5455): mask is PREFIX-LM not bidirectional - prompt query CAUSAL over prompt-only (k<P, never attends canvas), canvas query bidirectional over all. => prompt per-layer K/V is a PURE FUNCTION OF PROMPT, INVARIANT across the ~48 denoise steps -> cache prompt KV once, denoise only ~8-tok canvas against it. 48x1852 -> 1x1852+47x8 = ~40x, byte-exact (gate = confirmation not risk), reuses persistent-KV ABI. SWA reads a window of same cached prompt KV; RoPE invariant. BUILD (fresh ctx): step1 full fwd save prompt KV[0,P); steps2-48 skip prompt tower, canvas-only; gate G-DG-N5c-VAL expect identical picks; SP_DG_PREFIXKV default-off. REJECTED: MTP-diffusion graft (retrain heads + wrong FLOP direction dense12B>A4B + spec acceptance-gated + heads downstream of wall). Cheap floor: bisect SP_DJ_STEPS for pick-stabilization. Build on fresh context.
timestamp: 2026-06-22T09:06:11Z
resource: TBD
sp_status: DESIGN
sp_gate: none
sp_commit: TBD
sp_repro: none
mem_kind: agent
mem_addr: d8c692fa5cb29e53
tags: [diffjudge prefix-kv, prefix-LM mask, prompt tower invariant, byte-exact 40x, k_attn_diffusion, cache prompt KV, canvas only loop, persistent-KV ABI reuse, MTP graft rejected, launchpad next session, phase5 final optimization, SP_DG_PREFIXKV, agent, tier-2]
mem_tier: full
---

ï»¿# DIFFJUDGE PREFIX-KV REUSE â€” verified byte-exact ~40x lever (LAUNCHPAD for next session build)

## Verdict
The diffusion judge re-runs the FULL n_tok~1852 forward every one of ~48 denoise steps, but ~1844
of those tokens are the FIXED prompt. The mask makes the prompt tower a MATHEMATICAL CONSTANT across
steps -> cache prompt K/V once, denoise only the ~8-tok canvas against it. 48x1852 -> 1x1852 + 47x~8
= ~40x, and it is BYTE-EXACT (not an approximation). Reuses the persistent-KV ABI we already built.

## The proof (read in tree, NOT guessed) â€” cuda_forward.cu k_attn_diffusion ~line 5455-5469
Header says "bidirectional" loosely; the ACTUAL mask is PREFIX-LM:
  - prompt query -> CAUSAL over PROMPT ONLY: keys k<=t with k<P. Prompt NEVER attends to canvas.
  - canvas query -> bidirectional over ALL prompt + ALL canvas (sliding layers: last n_swa-1 prompt
    positions + all canvas).
Because prompt queries only attend to prompt keys (k<P), every prompt token's per-layer K/V is a
PURE FUNCTION OF THE PROMPT -> INVARIANT as the canvas denoises. Canvas noise has zero back-prop into
the prompt tower. So prompt K/V computed at step 1 are reusable VERBATIM steps 2..48 = byte-exact.
SWA layers DON'T break it (canvas reads a WINDOW of the same cached prompt KV; prompt KV still
prompt-only). RoPE on prompt KV is at fixed [0,P) positions -> invariant. Split point P = n_tok -
canvas_length; canvas = last canvas_length positions. The judge reads ONLY the canvas logits.

## Build blueprint (fresh context)
1. Step 1 (init): full [prompt|canvas] forward (n_tok=1852); SAVE each layer's prompt K/V rows [0,P)
   into the persistent KV cache.
2. Steps 2..48 (fast loop): SKIP the prompt tower; recompute only canvas rows [P,n_tok): canvas embed
   -> canvas attn (Q_canvas attends to cached prompt KV[L] + fresh canvas KV[L]) -> canvas FFN ->
   canvas logits. Prompt-tower FFN/attn (the 1844-wide cublasSgemm) is NOT run after step 1.
3. Gate: G-DG-N5c-VAL selection parity. Because the cache is byte-exact, EXPECT identical picks / zero
   deflection -> the speed win IS its own seal. Default-off env (e.g. SP_DG_PREFIXKV) = null floor.
4. Build CUDA (build-cuda, test_dg_forward_timing for per-forward; test_diffjudge_denoise for the
   pick gate); SM-pin + fresh-process + warmup for any timing.

## Honest negative (do NOT re-propose) â€” MTP-DIFFUSION GRAFT REJECTED
Gemini proposed grafting diffusion onto a 12B with MTP heads ("temporal step-drafting", k heads
predict denoise steps s-1..s-k in parallel -> 48->12 forwards). REJECTED, 4 reasons:
 (1) = speculative decoding on the denoise trajectory; speedup = acceptance_rate x k, NOT k. s-2
     depends on s-1's output; drafting+verifying is unmeasured + likely low-acceptance mid-trajectory.
 (2) needs RETRAINING: Gemma MTP heads predict t+1..t+k TOKENS, not denoise states. "Remap to
     diffusion objective" = train new heads + a diffusion loss on a 12B. Not a graft.
 (3) FLOP direction backwards: 26B is A4B (~4B active/token); dense 12B activates all 12B -> dense
     12B is ~3x MORE compute/token than the A4B, not less. Only the 2B cuts FLOPs (at OOD-recall cost).
 (4) MTP heads are DOWNSTREAM of the measured trunk-GEMM wall; distributing canvas-eval across them
     doesn't cut the trunk re-compute that IS the bottleneck.
Prefix-KV (above) gets the win with zero training, right-direction, byte-exact, owned infra.
If speculation is ever wanted: the LOSSLESS cross-model spec (2B drafts, 26B verifies) = task #319,
no new heads/training; dominates MTP-head-spec.

## Cheap floor (orthogonal, also free): bisect SP_DJ_STEPS for the min steps where the PICK
stabilizes (argmax over candidates converges before the full answer does); recall-parity-gated.

## Context note: discovered at session end (Phase 5 sealed). Build on FRESH context to avoid FFI/
lifecycle regressions. The cut is de-risked + locked. NEXT = the prefix-KV loop, then it becomes the
NIGHTSHIFT offline Stage-2 consolidation adjudicator (nightshift_curator.rs, do-not-rebuild).
