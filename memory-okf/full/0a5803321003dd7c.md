---
type: memory
title: FULL store-merge GREEN: engine loads harness-written memory-okf concepts + serves per OKF policy (G-STORE-MERGE 3/3)
description: SP_MEM_OKF_STORE=<root>: at boot the engine loads memory-okf/full/*.md concepts (text+policy), mints their L5 selection key from the body (question-space, cached full/<addr>.l5), pushes text-delivery Episodes into nightshift. GATE 3/3: 2 concepts written by okf_mem.py add (no engine), engine recalled+served counterfact->systemecho ('Mount Kea'), secret->recite ('Z7Q-3RT-9WX') + zero-inference DECLINE on absent ('Who built Falcon-9?'). Bidirectional with #72 (engine writes ep.okf.md): ONE content-addressed policy-bearing store, both callers - engine EXECUTE, harness Nexus INGEST, agency/NIGHTSHIFT CURATE, L5/vector/qmd RETRIEVE. recall.rs parse_okf_policy/okf_body; routes.rs load_and_mint_okf_store; daemon.rs hook. Default-off null floor. DO NOT rebuild. Follow-on: hot-reload while running; cache invalidation on edit.
timestamp: 2026-07-03T10:44:02Z
resource: engine 6c84ca1
sp_status: GREEN
sp_gate: G-STORE-MERGE
sp_commit: engine 6c84ca1
sp_repro: okf_mem.py add --root <store> --mem-class ...; run_merge.bat; merge_run.py
mem_kind: agent
mem_addr: 0a5803321003dd7c
mem_class: fact
mem_delivery: recite
mem_authority: supplements
mem_confidence: 0.9
tags: [store-merge, SP_MEM_OKF_STORE, load_and_mint_okf_store, harness-writes-engine-serves, one-store, unify, bidirectional, l5-mint-at-boot, agent, tier-2]
mem_tier: full
---

ï»¿G-STORE-MERGE (#76/#77/#78) â€” the FULL store-merge: a memory the HARNESS writes into the
shared MEM-OKF store is instantly RECALLED + SERVED by the engine per its OKF policy. One
content-addressed store, both callers. (2026-07-03)
=========================================================================================
The last big piece of "the whole system as one". Previously the engine served its own
registry.jsonl + latent episode dirs, while the harness/agent (Nexus, okf_mem) wrote text
concepts into memory-okf/full/*.md â€” two stores. Now the engine READS the shared store.

BUILD (engine this-commit, gated SP_MEM_OKF_STORE=<root>, default-off = null floor):
  * recall.rs: parse_okf_policy(str) + okf_body(str) â€” parse a MEM-OKF concept's policy
    frontmatter + delivery body from a string (load_episode_okf_policy now delegates to it).
  * routes.rs: load_and_mint_okf_store(app, root) â€” for each memory-okf/full/<addr>.md:
    parse policy + body; load the L5 selection key from a cached sidecar (full/<addr>.l5) or
    MINT it from the body at startup (question-space, mint_question_l5) and cache it; build a
    text-delivery Episode (gk empty, l5key set, policy from the concept) and push into
    app.nightshift so the live L5 recall path selects it.
  * daemon.rs: after the model boots + AppState is built, if SP_MEM_OKF_STORE set, call the
    loader and log the merge count.

GATE â€” harness WRITES, engine SERVES. Two policy-bearing concepts written by the harness via
`okf_mem.py add` into _merge_store (NO engine involvement in the write):
  counterfact/systemecho : "The tallest mountain on Earth is now Mount Kea."
  private-secret         : "The launch code for Falcon-9 is Z7Q-3RT-9WX."
Engine booted with SP_MEM_OKF_STORE=_merge_store + SP_MEM_POLICY=1 + empty registry. Boot log:
  "QKEY: â€¦ <- question-space key from generated Q: 'What is the tallest mountain on Earth?'"
  "QKEY: â€¦ <- 'What is the launch code for Falcon-9?'"
  "STORE-MERGE: loaded 2 memory-okf concept(s) â€¦ (2 L5 keys minted, rest cached)"
Then queried (auto_recall) â€” RESULT 3/3:
  [counterfact/systemecho ] "tallest mountain now?" -> "â€¦is now Mount Kea"        OK
  [private-secret/recite  ] "Falcon-9 launch code?" -> "â€¦is Z7Q-3RT-9WX"          OK
  [private-secret/decline ] "Who built Falcon-9?"   -> "I have a record for that
                                                        entity, butâ€¦" (zero-inf)  DECLINE

VERDICT: GREEN. The engine loads harness-written MEM-OKF concepts, mints their selection keys
at boot, recalls them by L5, and serves each per its OWN OKF policy (systemecho / attr-gate
zero-inference decline). Combined with #72 (engine WRITES ep.okf.md concepts) the store is now
bidirectional: harnessâ†’engine (this gate) + engineâ†’harness (the ep.okf.md sidecar). The whole
system reads and writes ONE content-addressed, policy-bearing store â€” engine spine (EXECUTE),
harness Nexus (INGEST), NIGHTSHIFT/agency (CURATE), L5/vector/qmd (RETRIEVE) â€” all one format.
Default-off preserved (SP_MEM_OKF_STORE unset = the engine's own registry only, byte-identical).

FOLLOW-ONS (smaller): re-mint the L5 key on concept edit (cache invalidation by content hash);
a live "harness adds while engine runs" hot-reload (today the merge is at boot); finer classes.

REPRO: okf_mem.py add --root _merge_store --mem-class â€¦ ; run_merge.bat ; python
_merge_store/merge_run.py. Boot log _merge_serve.log. ENV: engine this-commit; RTX 2060 12GB.
