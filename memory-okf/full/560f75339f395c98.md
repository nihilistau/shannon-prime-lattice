---
type: memory
title: DO NOT REBUILD: global-eviction slab + LSH router + NIAH already CLOSED GREEN (XBAR P3.2-b-2b / Phase C, 2026-06-13/14)
description: 8x global KV eviction slab + learned-LSH router + NIAH far-needle retrieval are ALREADY built+trained+gated in gemma4_decode_cuda. Trained weights on disk: tests/fixtures/lsh/lsh_R_r32_raw.bin (SP_ARM_LSH_R) + lsh_M_r32.bin (SP_ARM_LSH). C-c NIAH GREEN: HIT 837492 at depth 10/50/90% with the learned router; FROZEN +-1 = MISS. Do NOT retrain the router or rebuild the slab; a daemon-path NIAH MISS is a PORT bug (not loading lsh_R_r32_raw.bin), not router quality.
timestamp: 2026-06-30T13:57:24Z
resource: 222463a,33ac632,8e35877,3218d73
sp_status: PROVEN
sp_gate: G-P3-R2.b-2b-LSH, G-P3-R2.b-2c, C-c-NIAH
sp_commit: 222463a,33ac632,8e35877,3218d73
sp_repro: SP_ARM_LSH_R=tests/fixtures/lsh/lsh_R_r32_raw.bin + SP_ARM_SLAB via test_gemma4_cuda SP_G4_NIAH
mem_kind: agent
mem_addr: 560f75339f395c98
tags: [global eviction slab,  LSH router,  R.bin,  lsh_R_r32_raw.bin,  lsh_M_r32.bin,  NIAH,  needle,  SP_ARM_SLAB,  SP_ARM_LSH,  SP_ARM_LSH_R,  SP_ARM_GATHER,  O(1) KV,  daemon g4_kv_step,  XBAR P3,  global sparse recall, agent, tier-2]
mem_tier: full
---

# DO NOT REBUILD: global-eviction slab + LSH router + NIAH (XBAR P3 — already CLOSED GREEN)

**Banked 2026-06-30 after the ~12th rebuild of this exact subsystem.**

## What already exists (PROVEN, on disk, gated)
- **Trained LSH router weights** in `shannon-prime-system-engine/tests/fixtures/lsh/`:
  - `lsh_R_r32_raw.bin` — 65536 B = 512×32×4, the r=32 projection **sidecar** (`SP_ARM_LSH_R`).
  - `lsh_M_r32.bin` — 1048576 B = 512×512×4, the M = R·Rᵀ form (`SP_ARM_LSH`).
  - `lsh_Wc_i16_s15.bin` — the B3-WC autonomous-recall head (separate, also trained).
- **Slab + gather + union + router** all implemented in `src/backends/cuda/cuda_forward.cu` `gemma4_decode_cuda` (~lines 2389–2411 + the gather body). Knob chain:
  `arm_gather=SP_ARM_GATHER` → `arm_lsh=SP_ARM_LSH && arm_gather` → `arm_lshr=SP_ARM_LSH_R && arm_lsh` → `arm_slab=SP_ARM_SLAB && arm_lshr`.
- **Trainer:** `tools/xbar_lsh/train_lsh.py` (forward-KL distillation of the true attention distribution + hard-negative hinge; oracle ceiling mass@256 = 0.923). Consumes the `SP_ARM_DUMP` kq_call{0,1,2}.bin corpus from `gemma4_decode_cuda`.

## The gates that closed it
- `G-P3-R2.b-2b-LSH` — learned-LSH 8× = **+0.47% PPL** (engine `222463a`).
- `G-P3-R2.b-2c` / Phase C-b.2 — `SP_ARM_SLAB` O(1) VRAM ladder, N=8192↔16384 ~50 MiB delta = **O(1) cache confirmed** (engine `33ac632`).
- **C-c NIAH** — needle **survives** the O(1) compaction at every depth: depth 10%@16k (gap 14729) HIT · 50%@8k HIT · 90%@16k HIT, all exact `837492`, **only with the learned router**; FROZEN ±1 = MISS (negative control). (engine `8e35877` / `3218d73`).

Record: `PPT-LAT-STATE.md` §P3.2-b-2b / Phase C / C-c; `CONTRACT-XBAR-P3`.

## The trap (what keeps happening)
A session rebuilds the whole slab+gather+union **in the daemon's `g4_kv_step` persistent-KV path** (a different code path from the proven `gemma4_decode_cuda` scoring/NIAH harness), runs a fresh NIAH harness (`_g_niah_natural.py`), gets a **MISS**, and concludes "the router needs recalibration" — then nearly **retrains an already-trained router**.

**The MISS is a PORT bug, not router quality.** The daemon rebuild either never loaded `lsh_R_r32_raw.bin` (ran with an untrained/placeholder projection) or its gather/union/remap diverged from the proven `gemma4_decode_cuda` one. The trained router gets NIAH HITs at every depth — proven.

## The correct move (if O(1) KV is wanted LIVE in the daemon serving path)
1. **Do NOT retrain / recalibrate** the router. `lsh_R_r32_raw.bin` is trained and works.
2. **Do NOT rebuild** the slab from scratch. PORT the proven `gemma4_decode_cuda` gather + union logic into `g4_kv_step`, and **load `lsh_R_r32_raw.bin`** into the daemon slab open path.
3. Diff the daemon port against the green reference until the daemon NIAH HITs match the C-c gate.

## Also note
- The capability (O(1) KV + NIAH retention on the 12B) is **already proven** via the scoring harness. "Live in the daemon serving path" is an *integration*, not a new capability — weigh it against the other KEYSTONE open edges (2-GPU byte-exact; Telepathy live cross-family forward; tiered recall faithfulness; T4 Frobenius of weights) before sinking more time.
