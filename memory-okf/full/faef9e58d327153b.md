---
type: memory
title: N5c-v3b expert residence cache (engine cd9a4f8) + THE REDIRE
description: N5c-v3b expert residence cache (engine cd9a4f8) + THE REDIRECT: cache ENGAGES (2340 tensors ~63% resident, budget-stop 4GB, rc=0, byte-identical) but buys ~50% at DECODE (n_tok=14: 4.25->1.93s) and only ~6% at PREFILL (n_tok=1852: 25.5->~24.8s). => the diffusion JUDGE at prefill width is GEMM-COMPUTE-bound NOT transfer-bound (1852-wide cublasSgemm FLOPs dwarf the 14GB H2D). The whole I/O-attack arc (N5b disk + N5c-v2 halve-H2D + N5c-v3 residence) CONVERGES: judge prefill wall is ARITHMETIC not PCIe. Cache ships as the byte-identical ~50% DECODE/chat win, gated SP_DG_WCACHE default-off. To cut the JUDGE: int8/tensor-core GEMM (cuts FLOPs, not byte-identical) OR fewer denoise steps OR smaller judge. G-DG-N5c-VAL parity re-run still the only outstanding seal.
timestamp: 2026-06-22T01:37:26Z
resource: TBD
sp_status: GREEN
sp_gate: none
sp_commit: TBD
sp_repro: none
mem_kind: agent
mem_addr: faef9e58d327153b
tags: [n5c-v3b, expert residence cache, prefill compute-bound, judge wall is arithmetic not PCIe, decode 50% win, prefill 6%, regime story, byte-identical, SP_DG_WCACHE, int8 tensor-core next, cd9a4f8, redirect, agent, tier-2]
mem_tier: full
---

ï»¿# N5c-v3b: expert residence cache + THE REDIRECT â€” the diffusion judge is prefill-COMPUTE-bound

Engine commit cd9a4f8 (lineage 160a9e5 -> cd9a4f8).

## What
Extended the resident DevTensor cache (N5c-v3) to the expert slices in dg_gemm_packed_rows
(key name#r0). Same budget-stop, no eviction. Default margin bumped 1536->3072 (gate ran 4096) so
the ~2.4GB LM-head dequant scratch is never starved. BYTE-IDENTICAL. Gated SP_DG_WCACHE.

## Gate (test_dg_forward_timing, SM pinned, fresh process/leg, NFWD=3)
ENGAGES: 2340 tensors resident through layer 18 (~63% of weights), budget-stop hit 4GB floor, rc=0.
  n_tok=1852 (judge):  off 27.5/25.5/25.5 ; on 29.0(cold)/24.8/25.4(warm) = ~0.7-1.5s saved (~6%)
  n_tok=14   (decode): on 4.25(cold)/1.93(warm) = ~2.3s saved (~50%)

## THE FINDING (redirect, the headline)
Caching ~63% of the weights buys ~50% at DECODE but only ~6% at PREFILL. Therefore the diffusion
judge at its real width (n_tok~1852) is GEMM-COMPUTE-bound, NOT transfer-bound: the ~25s is the
1852-wide cublasSgemm FLOPs across the experts; the ~14GB H2D overlaps with / is dwarfed by the
matmul. Eliminating H2D barely moves prefill because H2D was never the prefill wall.
The whole I/O-attack arc CONVERGES on this: N5b (disk, RED) + N5c-v2 (halve H2D, byte-identical) +
N5c-v3/v3b (weight residence) -- three transfer optimizations -- collectively PROVE the judge's
prefill wall is ARITHMETIC, not PCIe. Decode (chat generation, n_tok small) IS transfer-bound, so
the cache is a real ~50% byte-identical DECODE win -- just not the prefill-judge lever.

## NEXT (to actually cut the judge)
The prefill lever is FLOPs, not bytes: (a) int8/tensor-core batched GEMM (cuts compute, NOT
byte-identical -- precision/speed tradeoff the f32 gemm_w can't cross), (b) fewer denoise steps
(the entropy-bound sampler / 8-step cap), or (c) a smaller judge. The SP_DG_WCACHE residence cache
ships as the DECODE/chat win (byte-identical, gated, default-off). The G-DG-N5c-VAL selection-parity
re-run is still the only outstanding seal (v1 sealed @1e-3; all v2/v3 are byte-identical f32).
Receipt: tests/fixtures/chat_fullstack/G-DG-N5c-WCACHE-experts.log.
