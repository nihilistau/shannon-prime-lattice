---
type: memory
title: P3.2 DONE — OKV global-eviction slab scaffold (engine b57b0e6); P3.3 = g4_kv_step gather
description: P3.2 GREEN: slab alloc scaffold in cuda_forward.cu + daemon.rs on branch feat/okv-p3-global-evict (b57b0e6). g4_arm_open/g4_arm_close + sp_g4_kv slab fields + gemma4_kv_open/close hooks + SP_DAEMON_KVDECODE_SLAB/_LSH_R/_BSLAB knobs. Default-off branch => byte-identical null floor (gate G-P3.2: 6-turn chat slab-OFF == pre-P3.2 baseline SHAs, GREEN). CUDA+daemon rebuild clean, zero errors. Slab-ON registers w/o OOM (Bslab=1024). P3.3 NEXT = port the per-step gather from gemma4_decode_cuda (~3316-3460) into g4_kv_step global branch (~4181-4220): store->arm_ramK/V, mint RtK sidecar (k_proj_RT->arm_pk, index by global-index), score (k_qk_scores), host top-B (sp_oracle_top_b), union {sinks,pos,top-B}<=Bslab, stage+H2D+remap, k_attn_decode_gather. Gate G-OKV-P3-EVICT (byte-exact-when-resident locally; long-context NIAH still thrashes on 12GB).
timestamp: 2026-06-30T10:53:21Z
resource: engine b57b0e6
sp_status: GREEN
sp_gate: G-P3.2 (byte-identical-off)
sp_commit: engine b57b0e6
sp_repro: SP_DAEMON_KVDECODE_SLAB=1 + _LSH_R=_xbar/p2b/lsh_R_r32_raw.bin ; _g_persist_kv_gate.py slab-off == baseline
mem_kind: episode
mem_addr: 74a9b4099b5de25e
tags: [P3.2, OKV, global-eviction, slab, g4_arm_open, sp_g4_kv, arm_slab, gemma4_kv_open, P3.3, gather, byte-identical, episode, tier-2]
mem_tier: full
---

# P3.2 DONE — OKV global-eviction slab alloc scaffold (engine b57b0e6, branch feat/okv-p3-global-evict)

**Gate G-P3.2 (byte-identical-when-off): GREEN.** 6-turn chat with the slab OFF = byte-identical to the pre-P3.2 baseline (SHAs f3c73fe…→fd99510…, all match). CUDA lib (`build-host-cuda-backend.bat`) + daemon rebuilt clean; my code compiled with zero errors (the 3 nvcc/cl warnings are pre-existing lines).

## What landed (cuda_forward.cu + daemon.rs)
- `sp_g4_kv` slab fields: `arm_slab/arm_Bslab/arm_B/arm_sink/arm_r_dim`, `arm_ramK/V` (host store), `arm_dR/arm_pk/arm_Rq`, `arm_dscores/arm_scores_h`, `arm_stageK/V`, `arm_ri_host/m_host/ri_host2/ri_dev/m_dev/slotmap/union/seen`.
- `g4_arm_open(s)` (static, before `gemma4_kv_open`): loads LSH-R router (`SP_ARM_LSH_R`, r inferred from file size), computes `arm_Bslab` (default min(P,4096)), **caps GLOBAL owner `dKc/dVc` to Bslab slots** (frees the full-P alloc, re-allocs small), stands up host-RAM global store + the r-dim sidecar sized **n_global*P*r (VRAM-lean, not NL*P*r)** + union/stage scratch. Returns 0/-1.
- `g4_arm_close(s)` (NULL-safe) called from `gemma4_kv_close`.
- `gemma4_kv_open` hook: `if (getenv("SP_ARM_SLAB") && getenv("SP_ARM_LSH_R")) g4_arm_open(s)` before `return s`. Off ⇒ untouched.
- daemon.rs: `SP_DAEMON_KVDECODE_{SLAB,LSH_R,BSLAB,ARM_B,ARM_SINK}` → set the `SP_ARM_*` env at open (mirrors the ring knobs).
- Slab-ON: daemon registered w/o OOM at Bslab=1024 (R.bin `_xbar/p2b/lsh_R_r32_raw.bin`); the `[g4-kv] P3 ARM SLAB armed` fprintf is lazy (fires on first chat, not registration), so full slab-on alloc validation comes with P3.3.

## P3.3 (NEXT) — the per-step gather in g4_kv_step
Port the gather from `gemma4_decode_cuda` (cuda_forward.cu ~3316–3460) into `g4_kv_step`'s GLOBAL branch (~4181–4220 store + attention): (1) store new global K/V to `arm_ramK/V` (not dKc); (2) mint RᵀK sidecar per global pos via `k_proj_RT` into `arm_pk` (index by global-index gi, since sidecar is n_global-sized); (3) score `k_proj_RT(q)→arm_Rq` + `k_qk_scores` on the sidecar; (4) host top-B (`sp_oracle_top_b`); (5) build union {sinks∪pos∪top-B} capped at Bslab, stage from RAM, H2D into the compact slab, remap abs→slot; (6) `k_attn_decode_gather`. Gate G-OKV-P3-EVICT: byte-exact when slab≥ctx; NIAH-cond-C HIT past the slab; 40-64K no-OOM. NOTE: still can't fully gate long-context on the 12GB card (thrash) — byte-exact-when-resident is the locally-runnable check.
