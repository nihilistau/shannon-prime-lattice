---
type: memory
title: N5c-v2 gemm_w BUILT+MEASURED+COMMITTED (engine 9cc65e2): win
description: N5c-v2 gemm_w BUILT+MEASURED+COMMITTED (engine 9cc65e2): wins BOTH widths under SM-locked control - n_tok=14 f32 68.3s->v2 3.37s (20.3x), n_tok=1852 f32 156.2s->v2 27.2s (5.74x), byte-identical f32. Corrects d1d90fe9: v1 GEMV loop inconclusive at prefill (788 vs 771); gemm_w (device-dequant->cublasSgemm) fixes the regime. Methodology now binding (SM-pin+fresh-process+warmup; mem-lock unsupported on WDDM 2060). PENDING: G-DG-N5c-VAL v2 selection-parity re-run (v1 sealed @1e-3; v2 byte-identical by construction).
timestamp: 2026-06-22T00:47:42Z
resource: TBD
sp_status: GREEN
sp_gate: none
sp_commit: TBD
sp_repro: none
mem_kind: agent
mem_addr: c1baec3fa80f6297
tags: [n5c-v2, gemm_w, prefill primitive, device dequant cublas, byte-identical f32, clock locked SM pinned, 20x decode 5.74x prefill, wins both widths, k_dequant_arena_q4b, reuse not rebuild, measured right, 9cc65e2, agent, tier-2]
mem_tier: full
---

ï»¿# N5c-v2: gemm_w prefill primitive (BUILT + MEASURED + COMMITTED)

Engine commit 9cc65e2 (lineage 2102d35 -> 9cc65e2).

## What changed
dg_gemm_packed + dg_gemm_packed_rows in src/backends/cuda/cuda_forward.cu rewritten from the
per-token gemv_w_packed LOOP (v1) to upload_packed -> gemm_w (k_dequant_arena_q4b device-dequant
to f32 scratch -> cublasSgemm). gemm_w (cuda_forward.cu:4974) is the DENSE 12B path's own packed
matmul; per the in-tree comment "dequant->SGEMM carries NO extra rounding for Q4B" => BYTE-IDENTICAL
f32 (tighter than v1's ~1e-3 dp4a). cublasHandle_t cb threaded through all 10 call sites (8 dense
FFN/attn/head + 2 MoE expert-slice). Dropped the dp4a-only in%32 guard on the dense path
(gemm_w needs no alignment). Gated SP_DG_PACKED (default-off = f32 null floor). NO new kernel.

## CONTROLLED measurement (binding methodology applied)
test_dg_forward_timing (widened: toks[2048] + SP_DGT_NTOK tiles the prompt to a target n_tok).
SM clock PINNED 2100 MHz (nvidia-smi -lgc 2100,2100); fresh process per leg (clean VRAM);
NFWD=3 (forward 1 warms cold cuBLAS/malloc, 2-3 steady-state). Mem-clock lock UNSUPPORTED on this
WDDM consumer 2060 (-lmc rejected) -> warmup carries the mem-clock settling (honest caveat).

  n_tok=14   (decode width):  f32 68.3s  -> v2 3.37s  = 20.3x
  n_tok=1852 (prefill width): f32 156.2s -> v2 27.2s  = 5.74x

v2 WINS AT BOTH WIDTHS. This is the correction to d1d90fe9: v1's GEMV loop was launch-overhead-bound
at prefill (788 vs 771, inconclusive); gemm_w fixes the regime. The f32 path is n_tok-independent
on the upload wall (~68s dequant+28GB-H2D every forward, no caching: 68.3@14 vs the 156@1852 adds
only the larger GEMM). v2 halves the H2D (14GB packed) + GPU-dequants, so it wins everywhere; the
prefill win (5.74x) is smaller than decode (20.3x) because the cuBLAS GEMM compute is shared by both
and dominates more at n_tok=1852. 48-step prefill-width judge query ~125min -> ~22min.
Receipt: tests/fixtures/chat_fullstack/G-DG-N5c-v2-TIMING.log + the 9cc65e2 commit message.

## Hybrid (mirrors the dense path)
GEMM-prefill (gemm_w, this) for wide n_tok; the GEMV loop stays the decode-width (n_tok=1) primitive.

## PENDING (final seal)
G-DG-N5c-VAL selection-parity for v2 specifically. v1 already SEALED identical picks (pick=-1,
ans_tok=1852, recall/reject identical) at ~1e-3; v2 is byte-identical f32 by construction (tighter),
so picks MUST be identical -> the re-run is confirmation, not risk. Long bake (judge over corpus).

## NEXT levers (operator's, post-v2)
expert CPU/GPU split; resident hot-expert VRAM LRU (26B is A4B => ~8 of 128 experts/token => a
resident hot set could erase most of the remaining per-forward expert H2D).
