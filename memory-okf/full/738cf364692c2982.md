---
type: memory
title: FAITHFULNESS SOLVED: recall-path obedience 100% (Jaccard select + text-in-context + faithfulness sys prompt)
description: Faithfulness axis CLEAR. Chain: F1 in-context 100% -> F1b.1 pure-KV replay 0% (selection+delivery gap) -> F2b/F3 Jaccard select + text-in-context = 100% (G-FAITHFUL-RECALL-JACCARD 15/15, 0.0pp cost). FIX (both needed): (1) select episode by recall::token_overlap (production Jaccard) not W_c (W_c is geometric, blind to natural facts); (2) deliver text-in-context (reset_cold+prefill aug+syn_last) WITH the faithfulness system prompt (without sys=33%, with=100%; pure-KV replay=0%). routes.rs SP_RECALL_JACCARD default-off. Rule: natural facts -> Jaccard+text; novel needles -> W_c+replay. Engine 184994b; receipts tests/fixtures/faithful/G-FAITHFUL-RECALL-JACCARD*. Supersedes RED e5444d2.
timestamp: 2026-06-30T19:21:51Z
resource: 184994b
sp_status: GREEN
sp_gate: G-FAITHFUL-RECALL-JACCARD (15/15)
sp_commit: 184994b
sp_repro: _faithful_serve_jaccard.bat (SP_RECALL_JACCARD=1 + registry) then SP_FAITHFUL_SEAM=jaccard python tests/perf/_g_faithful_recall.py
mem_kind: episode
mem_addr: 738cf364692c2982
tags: [faithfulness,  SOLVED,  F2b,  F3,  recall obedience 100,  Jaccard select,  token-overlap,  text-in-context,  faithfulness system prompt,  SP_RECALL_JACCARD,  G-FAITHFUL-RECALL-JACCARD, episode, tier-2]
mem_tier: full
---

# FAITHFULNESS SOLVED end-to-end — recall-path obedience 100% (F2b/F3 GREEN)

**Banked 2026-07-01. Engine main `184994b`. Supersedes the F2 RED entry e5444d2.**

## The full chain (receipts-first)
| stage | seam | obey | gate |
|---|---|---|---|
| F1 | in-context (fact in prompt) | 100% | G-FAITHFUL-BASELINE |
| F1b.1 | pure-KV replay (SP_B3_WC → kv::replay) | 0% | G-FAITHFUL-RECALL-WC (selection+delivery gap) |
| F2 | W_c select + text-in-context | 0% | wctext (W_c mis-selects natural facts) |
| **F2b/F3** | **Jaccard select + text-in-context + faithfulness sys prompt** | **100%** | **G-FAITHFUL-RECALL-JACCARD (15/15, 0.0pp cost)** |

## The fix (two parts, both needed)
1. **Selection**: `SP_RECALL_JACCARD` selects the episode by `recall::token_overlap(query, ep.text)` (the production Jaccard discriminator) instead of the W_c head. W_c is geometric — great for high-entropy novel needles, blind to mutually-similar natural-language facts (it picked query-independent episodes, 0/15). Token-overlap nails the right episode (overlap 0.5–0.75, 15/15 correct).
2. **Delivery**: deliver the selected episode as **text-in-context** (reset_cold + prefill the augmented prompt + syn_last), NOT pure-KV replay. CRUCIAL: the augmented prompt must include the **faithfulness system prompt** ("...use facts you were given faithfully..."), exactly F1's T_CONSOLE shape. Without the system message it was 33%; with it, 100%. (Pure-KV replay can't override strong priors at all — 0%.)

routes.rs: F2b branch (after the W_c branch, in the registry scope), `SP_RECALL_JACCARD=1` (default-off null floor). Launcher `_faithful_serve_jaccard.bat`; harness `_g_faithful_recall.py` (SP_FAITHFUL_SEAM=jaccard).

## Architecture rule banked
Recalled **natural-language facts** → select by token-overlap/Jaccard + deliver text-in-context (with faithfulness sys prompt) = 100% obey. **Novel high-entropy needles** → keep the W_c head + pure-KV replay (default; W_c discriminates them fine, no competing prior). Two recall regimes, two selectors/delivery — by data type.

## Status
**Faithfulness axis CLEAR.** Delivery solved (F1), selection solved (Jaccard), end-to-end recall obedience = 100% on a 15-fact strong-prior conflict set. Default-off; opt in via SP_RECALL_JACCARD.
