---
type: memory
title: OOD diffusion kill-test RESOLVED - DIFFUSION WINS; my earlier INVALID call was my own error
description: DIFFUSION WINS the OOD kill-test (94.4% vs W_c 28.3% @K8, reject 98%>=96.9%) -> Phase-5/N5b JUSTIFIED. My 'INVALID/no-tags' call was MY error: grepped the harness 40-char result-line (benign 'embeddings required...overriding' init warning) not the full reply (real tags K4N/X3K + <channel>thought, verbose-probe confirmed). parse_tag has no gt-fallback. LESSON: grep the verbose full capture not the truncated result-line; verify the verifier.
timestamp: 2026-06-21T16:49:35Z
resource: TBD
sp_status: GREEN
sp_gate: G-DIFFJUDGE-OOD-H2H
sp_commit: TBD
sp_repro: python tools/xbar_lsh/diffjudge_recall_test.py --verbose --limit 2 ...; grep the verbose FULL capture for _TAGPOOL [consonant][digit][consonant] tags
mem_kind: agent
mem_addr: 8c863dec97d079b3
tags: [diffjudge, G-DIFFJUDGE-OOD-H2H, diffusion wins, N5b justified, 40-char truncation, verify the verifier, result-line, verbose capture, embeddings required benign, agent, tier-2]
mem_tier: full
---

VERDICT (CORRECTED): the OOD diffusion kill-test (G-DIFFJUDGE-OOD-H2H) is RESOLVED and DIFFUSION WINS. At matched K=8 on the held-out OOD corpus the diffusion judge scored recall@1 94.4% / foreign-reject 98.0% vs W_c 28.3% / 96.9% = +66.1pp recall (far outside the +/-11% N=72 noise) at >=96.9% reject -> it clears the pre-registered kill-criterion (+10pp recall AND >=96.9% reject). Phase-5 native diffusion-judge lane (incl. N5b resident reservoir) is JUSTIFIED. Architecture: W_c = cheap in-distribution Stage-1; diffusion judge = zero-shot Stage-2 adjudicator over the W_c/LSH top-K - exactly the structural role the W_c-memorizer finding demanded for NIGHTSHIFT's OOD new episodes.

ANALYST-ERROR CORRECTED: my first pass called this run INVALID ("model emitted no tags; 94.4% is a harness ground-truth-leaking artifact"). That was wrong, and the error was MINE, not the oracle's. Root cause: I grepped the harness per-query RESULT LINE, which prints only reply.strip()[:40] - and the first 40 chars are a BENIGN llama.cpp init warning ("W init: embeddings required but some input tokens were not marked as outputs -> overriding"). The real model output lives PAST the 40-char cutoff. parse_tag() operates on the FULL reply (strips the <|channel>thought block, matches the chosen tag in the post-thought answer) and has NO ground-truth fallback - got=<int> can only mean a real tag match, and got=gt for 17/18 is impossible without real judgment. A verbose 2-query probe (_diffjudge_probe.log) confirmed it directly: the full capture contains real _TAGPOOL tags (K4N X3K Z6K T5D...), the <|channel>thought reasoning block, and active denoising (diffusion step: 7/48). So BOTH runs are valid: G-DIFFJUDGE-1 (div, K=12) 95.6% and this OOD run (K=8) 94.4%.

NB on scope: 94.4% is the EXTERNAL 26B oracle (llama-diffusion-cli, PR-24423) used as the capability proxy. Our NATIVE single-forward diffusion judge was ~25%; N5b (resident-weight reservoir) + the iterative entropy-bound denoise sampler is the build that makes our native judge fast + accurate enough to match the proxy.

LESSON (durable): when verifying a harness pass, grep the VERBOSE full model capture for the expected token signature - NEVER the truncated result-line display. The pre-registered run-health caveat was right to demand verification; I then mis-executed the verification by checking truncated data. Verify the verifier.
