---
type: memory
title: P3.4 NIAH-past-slab: VRAM scaling PROVEN (52K opens); retrieval HONEST-NEGATIVE (global union lacks recent window)
description: VRAM/eviction PROVEN: Pmax=52000+slab opens 1193 MiB free (globals capped to Bslab not Pmax); 2776-tok flood (5x slab) runs no-OOM ~1.6GB free. RETRIEVAL FAILS (honest negative): NIAH needle@~177 in ctx 2776 MISSES + degenerate output '`' at BOTH Bslab=512 (clip) AND 2048 (no clip). ROOT CAUSE: global slab union = {sinks U pos U LSH-topB}, NO recent-global window -> the 8 global layers lose recent context (only SWA gets recent via ring) -> degenerate regardless of Bslab; LSH also doesn't fish the needle. Earlier 'resident coherent' was ctx<256 (union=all, no real eviction). FIX: add recent-W to the union {sinks U recent-W U topB U pos}; then validate/retrain the LSH router for this gemma4-12b global-K regime (cf B3-WC: relevance signals fail until trained right). G-OKV-P3-EVICT: VRAM half green, retrieval half red. Engine 0478345 + gather in feat/okv-p3-global-evict.
timestamp: 2026-06-30T12:50:51Z
resource: engine 0478345 / feat-okv-p3-global-evict
sp_status: RED
sp_gate: G-OKV-P3-EVICT (VRAM green, retrieval red)
sp_commit: engine 0478345 / feat-okv-p3-global-evict
sp_repro: Pmax=52000 slab open (free 1193); _g_niah_daemon.py 256 1700 7 -> MISS '`' at Bslab 512 + 2048
mem_kind: episode
mem_addr: 2d341375089faec1
tags: [P3.4, NIAH, slab, eviction, LSH, union, recent-window, honest-negative, OKV, 52K, unbounded, episode, tier-2]
mem_tier: full
---

# P3.4 NIAH-past-slab — VRAM scaling PROVEN; semantic retrieval HONEST-NEGATIVE (union lacks recent window)

## What's proven (the eviction architecture's VRAM promise)
- Pmax=**52000** + slab (Bslab=4096, ring_W=1024) **opens with 1193 MiB free** — vs full-cache at Pmax=40000 sitting at 77 MiB (edge). The 8 globals are capped to Bslab (~128 MB) instead of growing to Pmax (~1.6 GB), so context *far past* the ~40K resident ceiling allocates with room to spare. No transient OOM.
- A ~2776-token flood (≈5× the slab) prefills + decodes WITHOUT OOM at ~1.6 GB free, slab + ring + host-RAM store all engaged (`ARM SLAB armed`, `RING mode`). The mechanism RUNS at real eviction scale.

## Honest negative (retrieval/coherence under real sparse eviction)
- NIAH (needle at ~token 177, ctx 2776 >> Bslab) **MISSES**, and the output is **degenerate (`'```'`)** at BOTH Bslab=512 (union clips) AND Bslab=2048 (union ~1030 < Bslab, no clip). So it is NOT mere clipping.
- ROOT CAUSE: the global slab union is `{sinks ∪ pos ∪ LSH-top-B}` with **no recent-global window**. Under the slab the 8 GLOBAL layers see only sinks + current + LSH-selected-far positions — they LOSE recent context (only the 40 SWA layers get recent, via ring_W). Global attention therefore degenerates regardless of Bslab, and the LSH router does not fish the needle either.
- Why earlier "resident" looked coherent: that run was ctx<256 (union = ALL positions, no real eviction). Coherence held only because nothing was actually evicted. Real eviction (sparse subset of a large ctx) breaks it.

## Fix (next, bounded)
1. Add a **recent-global window** to the union: `{sinks ∪ recent-W ∪ LSH-top-B ∪ pos}` (e.g. recent-W = ring_W). This should restore global-layer coherence under eviction — the most likely single fix.
2. Then re-evaluate the LSH router's needle selection on this gemma4-12b global-K regime; it may need retraining/validation (the R.bin is from the §3q test-bin regime). Cross-ref the B3-WC arc: hand/learned relevance signals fail open-world until trained on the right ablation/diversity signal.
3. Gate G-OKV-P3-EVICT: needle HIT past the slab + coherent output at ctx >> Bslab, no OOM. Today only the VRAM half is green.

Receipts: open at Pmax=52000 slab (1193 MiB free); two NIAH runs (Bslab 512 + 2048) both MISS/degenerate (176s prefill each). Engine `0478345` (cfg bridge) + the P3.3 gather (`c753145`/in-branch) on `feat/okv-p3-global-evict`.
