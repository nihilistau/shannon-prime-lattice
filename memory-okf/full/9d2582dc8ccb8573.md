---
type: memory
title: N5c grounded: f32 baseline 78s/forward; flip diffusion forward onto gemv_w_packed+upload_packed (reuse, not new kernel)
description: MEASURED (test_dg_forward_timing, engine 621bc74): f32 diffusion forward = ~78s (OFF 79.1/77.7s warm). N5b reservoir is pure OVERHEAD (ON 82.4s warm > 78s OFF; cache didn't even hit - 266 clones/2fwd) -> ABANDON host reservoir. N5c = flip the 8 dg_upload_arena_w(f32 dequant+28GB H2D)->gemm(cb) sites in diffusion_gemma_forward_cuda (Wq/Wk/Wv/Wo/gate/up/down/head) onto the EXISTING packed path: upload_packed(14GB) + gemv_w_packed (dp4a, device dequant). NUANCE: gemv_w_packed is single-token GEMV, diffusion is n_tok GEMM -> n_tok-loop the GEMV (or write a packed GEMM). NO new kernel for the GEMV route. Win = half H2D (14 vs 28GB) + GPU dequant not CPU. Parity ~1e-3 (= dense dp4a path). Then map sp_forward_dispatch_fn + Rust ABI streaming. Instrument test_dg_forward_timing committed (runaway-proof).
timestamp: 2026-06-21T20:13:05Z
resource: engine 621bc74
sp_status: GREEN
sp_gate: G-DG-N5c
sp_commit: engine 621bc74
sp_repro: SP_DGT_NFWD=2 build-cuda/tests/test_dg_forward_timing.exe; OFF=78s ON=82s in _n5c_timing.log
mem_kind: agent
mem_addr: 9d2582dc8ccb8573
tags: [n5c, diffusion judge, f32 baseline, 78s per forward, gemv_w_packed, upload_packed, dp4a, packed GEMM, n_tok loop, dg_upload_arena_w, routing flip, reuse not rebuild, agent, tier-2]
mem_tier: full
---

N5c GROUNDED + BASELINE MEASURED (the receipt the runaways never gave).

BASELINE (test_dg_forward_timing, cudaEvent, CL=8, n_tok=14):
  reservoir OFF (f32 path): forward1 79108.6 ms, forward2 (warm) 77719.5 ms  => ~78s/forward
  reservoir ON  (host clone): forward1 94253.0 ms (cold +clone), forward2 (warm) 82421.8 ms
=> N5b host reservoir is PURE OVERHEAD: warm-ON 82.4s > OFF 77.7s by ~5s (14GB of host clones = memory pressure, zero benefit). Second defect: the at-keyed cache did NOT hit across forwards (266 clones for 2 forwards = re-cloning every forward). ABANDON the host reservoir. At ~78s/forward x 48 steps ~= 62 min/query, the wall is the per-forward f32 dequant + 28GB H2D, NOT disk.

THE FLIP (N5c, reuse not rebuild): the diffusion forward (diffusion_gemma_forward_cuda, ~line 6160-6260) runs 8 weights/layer through the f32 pattern:
    dW = dg_upload_arena_w(m, name, IN, OUT);   // CPU dequant 14GB->28GB f32 + 28GB H2D
    gemm(cb, dW, dnx, dout, n_tok, IN, OUT);     // cublas f32 SGEMM
    cudaFree(dW);
  (sites: attn_q/attn_k/attn_v/attn_output/ffn_gate/ffn_up/ffn_down + the LM head). This is the ONLY thing making the diffusion judge slow.
  REPLACE with the EXISTING packed dp4a path (the same one that keeps the dense 12B AR path fast):
    upload_packed(&at->pt, &devW);               // packed OK_Q4B H2D = 14GB (HALF), no CPU dequant
    for t in 0..n_tok: gemv_w_packed(st, &devW, dnx + t*IN, dout + t*OUT, dqx, dsx);  // dp4a, device dequant
    free_devtensor(&devW);
  gemv_w_packed (cuda_forward.cu:1700/5017) + upload_packed (1416) + k_quant_act_int8 ALL EXIST. NUANCE: gemv_w_packed is single-token GEMV; diffusion is multi-token GEMM => either loop the GEMV over n_tok (weight resident in VRAM during the loop, read at VRAM bandwidth) or write a packed multi-token GEMM (optional optimization). The GEMV-loop needs NO new kernel. Parity ~1e-3 vs f32 (int8 activation-quant + integer reduction order), identical to the dense dp4a decode path.
  EXPECTED: per-forward time drops materially (H2D halved + CPU dequant eliminated). Measure on test_dg_forward_timing (OFF f32 78s vs the packed path).

SEQUENCE (operator-ratified): (1) baseline DONE = 78s. (2) flip ONE site (e.g. ffn_gate/up/down) behind an env flag, measure on the harness, confirm parity + speedup. (3) flip the rest. (4) THEN map sp_forward_dispatch_fn (L1 6) + wire the sp_daemon Rust ABI to stream the packed bytes (cpu_forward_dispatch.rs trampoline pattern). (5) VRAM hot-expert LRU.

Instrument: test_dg_forward_timing.c (engine 621bc74) - 1-forward cudaEvent, bounded, runaway-proof (test_diffjudge_* ignore SP_DJ_LIMIT). cuda_forward.cu N5b reservoir telemetry left in (gated SP_DG_RESERVOIR, default-off) but the reservoir itself is to be removed/superseded by the packed flip.
