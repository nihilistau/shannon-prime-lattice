---
type: memory
title: CORRECTION: served KV compute IS foundation-routed (measured); only Spinor COMPRESSION is missing
description: MEASURED: byteexact default-on serves coherent, float path garbage -> served KV compute IS exact-integer O_K (via bx kernel + L1 ABI). 'Raw-float bypass' was wrong (static inference). Real gap = KV storage not Spinor-COMPRESSED (C2 envelope) = an optimization, not a bypass. Reframe the contract/guardrail.
timestamp: 2026-06-28T06:18:00Z
resource: a69fac7
sp_status: GREEN
sp_gate: measured-A/B-served
sp_commit: a69fac7
sp_repro: none
mem_kind: agent
mem_addr: 54b317e18803e3d5
tags: [CORRECTION, served KV measured, byteexact default, foundation routed, not a bypass, C2 compression gap, k_attn_decode_ring_bx, measure not grep, CONTRACT-CUDA-KV-FOUNDATION reframe, agent, tier-2]
mem_tier: full
---

ï»¿CORRECTION (measured 2026-06-28, refutes the earlier 'CUDA KV bypasses the foundation' fact): the served 12B decode KV COMPUTE IS routed through the exact-integer O_K foundation. MEASURED via the L1 ABI served path: byteexact default-ON => coherent ('The capital of Japan is Tokyo.'); raw_logits/float Stage-A => GARBAGE. The resident attention kernels k_attn_decode_ring_bx read the float-stored K/V (const float* Kc/Vc) and run the exact-integer dual-prime CRT-NTT (the ABI runs not-natively-exact inputs through the byte-exact O_K process, by design). So 'raw-float bypass of the foundation' was WRONG (static-grep inference, not measured). The ONLY real gap: the KV STORAGE is uncompressed float, i.e. NOT in the C2 Spinor/two-ring COMPRESSION envelope. That is a memory/effective-context OPTIMIZATION (lossless, O_K Spinor is byte-exact), gated by footprint + bit-exact -- NOT a correctness/bypass fix. CONTRACT-CUDA-KV-FOUNDATION + G-FOUNDATION-ROUTING premise must be reframed from 'stop the bypass' to 'add C2 Spinor compression to the already-exact served KV storage'. Lesson: measure through the ABI before asserting from grep.
