---
type: memory
title: G-PERSIST-KV GREEN — O(1) conversation KV gated + default-ON (engine d211fd2)
description: GATED + DEFAULT-ON. 6-turn chat vs live 12B: SP_PERSIST_KV off(O(n)) vs on(O(1) suffix-append) = ALL 6 turns byte-identical (matching sha+text). TTFT off grows 7.47x (1.35->10.1s), on flat ~1s. Daemon log: reuse 172/172 committed, prefill suffix 20, drop 0. Default-config run == off baseline byte-identical => flip safe. routes.rs persist_kv now != Some(0) (default-on; =0 = O(n) null floor); cache-mutating paths still excluded. Harness tests/perf/_g_persist_kv_gate.py + _persist_gate_compare.py; receipts _persist_gate_{off,on,def}.json. Runs on SWA ring W=2048 so persist x ring covered. P2 (bigger Pmax/ring ~40K) + P3 (global eviction to Optane) still open.
timestamp: 2026-06-30T06:03:02Z
resource: engine d211fd2
sp_status: GREEN
sp_gate: G-PERSIST-KV
sp_commit: engine d211fd2
sp_repro: _persist_serve.bat {0|1|empty} ; _g_persist_kv_gate.py <tag> ; _persist_gate_compare.py
mem_kind: episode
mem_addr: bb2f5a138b381fb8
tags: [persistent-kv, SP_PERSIST_KV, G-PERSIST-KV, O1, conversation, kvdecode, TTFT, default-on, gemma4, GREEN, episode, tier-2]
mem_tier: full
---

# G-PERSIST-KV GREEN — O(1) conversation KV gated + default-ON (2026-06-30)

**Result:** the persistent O(1) conversation KV (`SP_PERSIST_KV`) is GATED and now DEFAULT-ON. Engine commit `d211fd2`.

## Gate (G-PERSIST-KV)
6-turn scripted chat against the live 12B (`tests/perf/_g_persist_kv_gate.py`, temp 0, byteexact, SWA ring W=2048, Pmax=4096), run with `SP_PERSIST_KV=0` (O(n) re-prefill baseline) vs unset/on (O(1) suffix-append):

- **Byte-identity: GREEN — all 6 turns identical** (matching sha16 + text + char counts) off vs on. Comparator `tests/perf/_persist_gate_compare.py` exit 0.
- **TTFT telemetry:** off = [1351,2294,4034,5522,7748,10096] ms (grows **7.47×**); on = [1539,984,1074,1142,1418,1347] ms (**flat, 0.88×**). By turn 6 on is ~7.5× faster and the gap widens with length.
- **Mechanism proven in the daemon log:** `PERSIST-KV: reuse 172 of 172 committed (drop 0); prefill suffix 20 (full would be 192)` — pure strict-prefix append, no rewind.
- **Default flip confirmed:** a third run with NO `SP_PERSIST_KV` set (true default) = byte-identical to the off baseline (same 6 shas) with flat TTFT ⇒ default-ON is safe.

## Change
`routes.rs run_kvdecode_chat`: `persist_kv = env("SP_PERSIST_KV") != Some("0")` (was `== Some("1")`). Default-ON; `SP_PERSIST_KV=0` forces the O(n) re-prefill null floor. The cache-mutating paths (replay / single_entry / inject_frames / SP_DECIDE / SP_FORGET / SP_B4_NIGHTSHIFT / SP_B3_JUDGE / SP_B3_DISPOSER / SP_INT2) still hard-exclude persist and reset every turn. auto_recall stays allowed (non-committing read).

## Scope / honest notes
- Covers the **plain decode path** on the 12B resident cache (the kvdecode backend). The excluded paths fall back to reset (correct, O(n) that turn).
- `KV_COMMITTED` is a single global sequence ⇒ one active conversation at a time; a `chat_id` switch LCP-misses → full reset (correct, just not O(1) across interleaved chats).
- `drop_n > REWIND_BOUND(32)` → full reset fallback (correct).
- Receipts: engine `tests/perf/_persist_gate_{off,on,def}.json`; commit `d211fd2`. Repro: `_persist_serve.bat {0|1|<empty>}` then `_g_persist_kv_gate.py <tag>` then `_persist_gate_compare.py`.

## Remaining (from the scope, NOT done here)
P2 = larger Pmax + ring tuning for ~40K-token sessions (ring already on at W=2048); P3 = global-layer eviction to the XBAR Ring-2/Optane demotion for unbounded context (the 8 global layers are the O(n) floor).
