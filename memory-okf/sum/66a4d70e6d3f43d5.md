---
type: memory
title: SP-SWARM L0 transport GREEN (QUIC+Ed25519 roster, reused engine quinn NOT libp2p): 2-node bidirectional convergence + tamper-reject + off-roster reject
description: tools/sp_swarm/src/transport.rs behind the transport feature. ANTI-REBUILD: reused the engine's proven QUIC (quinn 0.11 + rustls 0.23/ring from tools/sp_daemon/src/network/quic_shard.rs) instead of rust-libp2p — QUIC TLS1.3 IS the encrypted-authenticated channel (X25519/ChaCha20/AES); libp2p DHT/gossipsub overkill for a closed invite-only mesh (SWARM doc §2 allows 'libp2p noise OR WireGuard mesh'). Invite-only identity = app-level Ed25519 mutual challenge-response over the encrypted stream (sign peer nonce; unrostered id or bad sig dropped). have/want pull runs accept() (L1+L2+L3) on every object before write. G-SWARM-TRANSPORT-QUIC GREEN (2-node localhost, tests/transport.rs): A<-B converge pulled=3 (tampered rejected integrity-fail), B<-A converge pulled=2 (reverse), off-roster C rejected (conn dropped, 0 objects). Receipt tests/fixtures/swarm/G-SWARM-TRANSPORT-QUIC.log. NEXT: wire sp_swarm into sp-daemon behind a swarm feature (reuse --quic-port hook); multi-host beyond loopback; peer discovery only if past static invite-list; L4 C2 overlay. Anti-rebuild: L0 transport DONE; don't rebuild, don't add libp2p.
timestamp: 2026-07-01T12:51:24Z
resource: TBD
sp_status: ACTIVE
sp_gate: none
sp_commit: TBD
sp_repro: none
mem_kind: agent
mem_addr: 66a4d70e6d3f43d5
tags: [SP-SWARM, L0, transport, QUIC, quinn, rustls, ed25519, roster, mesh, convergence, tamper-reject, anti-rebuild, G-SWARM-TRANSPORT-QUIC, agent, tier-1]
mem_tier: summary
mem_full: 66a4d70e6d3f43d5
---

# SP-SWARM L0 transport GREEN (QUIC+Ed25519 roster, reused engine quinn NOT libp2p): 2-node bidirectional convergence + tamper-reject + off-roster reject

tools/sp_swarm/src/transport.rs behind the transport feature. ANTI-REBUILD: reused the engine's proven QUIC (quinn 0.11 + rustls 0.23/ring from tools/sp_daemon/src/network/quic_shard.rs) instead of rust-libp2p — QUIC TLS1.3 IS the encrypted-authenticated channel (X25519/ChaCha20/AES); libp2p DHT/gossipsub overkill for a closed invite-only mesh (SWARM doc §2 allows 'libp2p noise OR WireGuard mesh'). Invite-only identity = app-level Ed25519 mutual challenge-response over the encrypted stream (sign peer nonce; unrostered id or bad sig dropped). have/want pull runs accept() (L1+L2+L3) on every object before write. G-SWARM-TRANSPORT-QUIC GREEN (2-node localhost, tests/transport.rs): A<-B converge pulled=3 (tampered rejected integrity-fail), B<-A converge pulled=2 (reverse), off-roster C rejected (conn dropped, 0 objects). Receipt tests/fixtures/swarm/G-SWARM-TRANSPORT-QUIC.log. NEXT: wire sp_swarm into sp-daemon behind a swarm feature (reuse --quic-port hook); multi-host beyond loopback; peer discovery only if past static invite-list; L4 C2 overlay. Anti-rebuild: L0 transport DONE; don't rebuild, don't add libp2p.

Full context: [full/66a4d70e6d3f43d5.md](../full/66a4d70e6d3f43d5.md)
