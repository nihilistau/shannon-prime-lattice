---
type: memory
title: N5c-v3 resident DevTensor cache, DENSE-backbone first cut (e
description: N5c-v3 resident DevTensor cache, DENSE-backbone first cut (engine 160a9e5): mechanism GREEN - 206 dense tensors resident, byte-identical, rc=0, no OOM, VRAM-budget guard (9.0->6.7GB). Dense-only win SMALL at prefill (~1.2s/28s ~4%, dense backbone only ~2.4GB) but ~35% at decode (4.37->2.78s @n_tok=14). Localizes the real PCIe tax to the EXPERTS (~12GB, ~all 128 fire at prefill - confirmed trace cnt 322/638/703). Gemini 'upload dead unselected experts' premise WRONG: dispatch already skips cnt==0 (line 6440); tax is RE-uploading SELECTED experts every step. NEXT = expert LRU (extend same cache to dg_gemm_packed_rows, capacity-bound) = where the real prefill win lives. Gated SP_DG_WCACHE default-off=null floor.
timestamp: 2026-06-22T01:22:36Z
resource: TBD
sp_status: GREEN
sp_gate: none
sp_commit: TBD
sp_repro: none
mem_kind: agent
mem_addr: 1d899e7c932f543e
tags: [n5c-v3, resident devtensor cache, dense backbone, SP_DG_WCACHE, no eviction, budget-stop cudaMemGetInfo, 206 residents, mechanism green, dense win small, experts are the tax, expert LRU next, 160a9e5, gemini premise wrong cnt==0 skip, agent, tier-1]
mem_tier: summary
mem_full: 1d899e7c932f543e
---

# N5c-v3 resident DevTensor cache, DENSE-backbone first cut (engine 160a9e5): mechanism GREEN - 206 dense tensors resident, byte-identical, rc=0, no OOM, VRAM-budget guard (9.0->6.7GB). Dense-only win SMALL at prefill (~1.2s/28s ~4%, dense backbone only ~2.4GB) but ~35% at decode (4.37->2.78s @n_tok=14). Localizes the real PCIe tax to the EXPERTS (~12GB, ~all 128 fire at prefill - confirmed trace cnt 322/638/703). Gemini 'upload dead unselected experts' premise WRONG: dispatch already skips cnt==0 (line 6440); tax is RE-uploading SELECTED experts every step. NEXT = expert LRU (extend same cache to dg_gemm_packed_rows, capacity-bound) = where the real prefill win lives. Gated SP_DG_WCACHE default-off=null floor.

N5c-v3 resident DevTensor cache, DENSE-backbone first cut (engine 160a9e5): mechanism GREEN - 206 dense tensors resident, byte-identical, rc=0, no OOM, VRAM-budget guard (9.0->6.7GB). Dense-only win SMALL at prefill (~1.2s/28s ~4%, dense backbone only ~2.4GB) but ~35% at decode (4.37->2.78s @n_tok=14). Localizes the real PCIe tax to the EXPERTS (~12GB, ~all 128 fire at prefill - confirmed trace cnt 322/638/703). Gemini 'upload dead unselected experts' premise WRONG: dispatch already skips cnt==0 (line 6440); tax is RE-uploading SELECTED experts every step. NEXT = expert LRU (extend same cache to dg_gemm_packed_rows, capacity-bound) = where the real prefill win lives. Gated SP_DG_WCACHE default-off=null floor.

Full context: [full/1d899e7c932f543e.md](../full/1d899e7c932f543e.md)
