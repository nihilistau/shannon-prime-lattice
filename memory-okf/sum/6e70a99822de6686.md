---
type: memory
title: DF-B6 GREEN: harness curator deploys the 0.5B classifier - SELF-IMPROVEMENT LOOP CLOSED (G-DF-DEPLOY)
description: CONTRACT-DATAGEN-FINETUNE brick 6 GREEN (harness b8785b1). Deployment = HARNESS CURATOR (operator-confirmed, ADR-002 idle-vs-hot-path): classification is an offline Tier-1 decider, lives where the PyTorch adapter lives (harness), emits a clean mem_class label to the shared store, engine hot-reloads via LM-B1. datagen/mem_class_curator.py: MemClassCurator loads the promoted adapter (Qwen2.5-0.5B + LoRA) on CPU (CUDA_VISIBLE_DEVICES empty - never touches engine GPU), classify() + curate_store() = drop-in replacement for the engine 12B model_classify (safety-monotone: never downgrades private-secret; Python mirror of rewrite_okf_class); active_adapter() resolves the promoted repo from datagen/model_registry.json (DF-B4). run_agency _start_mem_curator = background thread, gated SP_MEM_CURATOR=1 default-off. GATE G-DF-DEPLOY: CPU classify acc 0.833 (reproduces DF-B4 in the harness on CPU) + corrects counterfact->private-secret + counterfact->persona + safety-monotone holds on a genuine secret + rewrites disk. THE LOOP IS CLOSED: telemetry(LM-B2) -> convert(DF-B1)+seed(DF-B2) -> train(DF-B3 cloud) -> eval/promote(DF-B4) -> DEPLOY(DF-B6) -> deployed classifier curates the store the engine serves -> feeds next telemetry. adapter stays on HF (registry-resolved), not committed. DO NOT rebuild. FOLLOW-ON: live curator+engine-reconcile in one run; 12B-parity A/B; DF-B5 auto-train trigger; engine-native LatentHead/telepathy rungs if in-forward classify needed.
timestamp: 2026-07-03T16:47:15Z
resource: harness b8785b1
sp_status: GREEN
sp_gate: G-DF-DEPLOY
sp_commit: harness b8785b1
sp_repro: python3 datagen/cloud/hf_pull_adapter.py; python tests/h_datagen_deploy.py
mem_kind: agent
mem_addr: 6e70a99822de6686
mem_class: fact
mem_delivery: recite
mem_authority: supplements
mem_confidence: 0.9
tags: [df-b6, deploy, curator, mem_class, adapter, harness, adr-002, loop-closed, cpu, safety-monotone, model_classify-replacement, self-improvement, agent, tier-1]
mem_tier: summary
mem_full: 6e70a99822de6686
---

# DF-B6 GREEN: harness curator deploys the 0.5B classifier - SELF-IMPROVEMENT LOOP CLOSED (G-DF-DEPLOY)

CONTRACT-DATAGEN-FINETUNE brick 6 GREEN (harness b8785b1). Deployment = HARNESS CURATOR (operator-confirmed, ADR-002 idle-vs-hot-path): classification is an offline Tier-1 decider, lives where the PyTorch adapter lives (harness), emits a clean mem_class label to the shared store, engine hot-reloads via LM-B1. datagen/mem_class_curator.py: MemClassCurator loads the promoted adapter (Qwen2.5-0.5B + LoRA) on CPU (CUDA_VISIBLE_DEVICES empty - never touches engine GPU), classify() + curate_store() = drop-in replacement for the engine 12B model_classify (safety-monotone: never downgrades private-secret; Python mirror of rewrite_okf_class); active_adapter() resolves the promoted repo from datagen/model_registry.json (DF-B4). run_agency _start_mem_curator = background thread, gated SP_MEM_CURATOR=1 default-off. GATE G-DF-DEPLOY: CPU classify acc 0.833 (reproduces DF-B4 in the harness on CPU) + corrects counterfact->private-secret + counterfact->persona + safety-monotone holds on a genuine secret + rewrites disk. THE LOOP IS CLOSED: telemetry(LM-B2) -> convert(DF-B1)+seed(DF-B2) -> train(DF-B3 cloud) -> eval/promote(DF-B4) -> DEPLOY(DF-B6) -> deployed classifier curates the store the engine serves -> feeds next telemetry. adapter stays on HF (registry-resolved), not committed. DO NOT rebuild. FOLLOW-ON: live curator+engine-reconcile in one run; 12B-parity A/B; DF-B5 auto-train trigger; engine-native LatentHead/telepathy rungs if in-forward classify needed.

Full context: [full/6e70a99822de6686.md](../full/6e70a99822de6686.md)
