---
type: memory
title: O(1) conversation KV: SP_PERSIST_KV append ALREADY EXISTS (don't rebuild) + capacity numbers
description: SCOPED O(1) conversation KV. KEY ANTI-REBUILD FINDING: the cross-turn O(1) append ALREADY EXISTS = SP_PERSIST_KV (routes.rs run_kvdecode_chat, default-off): LCP-reuse of KV_COMMITTED + suffix-only prefill + bounded-tail rewind (REWIND_BOUND=32, SWA journal Jmax=64); byte-exact null floor when off; excludes replay/single_entry/inject/agency-writers/speculative-recall; auto_recall allowed (non-committing). The persistent-decode verb sp_session_register_kvdecode_backend is registered+gated (G-WIRE-CUDA-DECODE-GEMMA4 32/32 bit-id, VRAM flat). DON'T rebuild the append. Per-position KV = 4KB/owner-layer (global n_kv1*hd512, SWA n_kv2*hd256 both =512 floats K+V); kvfs=48 owners (gemma3 no KV-sharing); 8 global + 40 SWA (period6). Full-cache 192KB/pos; Pmax default 4096 (786MB). SWA ring (SP_DAEMON_KVDECODE_RING_W) bounds the 40 SWA layers O(1); 8 GLOBAL layers are the O(n) floor (evict via existing XBAR Ring-2/Optane demotion). Remaining work: gate G-PERSIST-KV (multi-turn byte-identity + tok/s) then enable; P2 ring+bigger Pmax (~40K tok); P3 global eviction. Coder delegate is CPU L1 (own session, no g_w) = isolated from the Gemma GPU cache.
timestamp: 2026-06-30T05:05:52Z
resource: lattice 8e9f041
sp_status: DRAFT
sp_gate: G-PERSIST-KV (pending)
sp_commit: lattice 8e9f041
sp_repro: none
mem_kind: agent
mem_addr: 55e022c67fb00d30
tags: [persistent-kv, SP_PERSIST_KV, O1, conversation, kvdecode, KV_COMMITTED, reprefill, ring, Pmax, sp_g4_kv, gemma4, scaling, OKV, agent, tier-1]
mem_tier: summary
mem_full: 55e022c67fb00d30
---

# O(1) conversation KV: SP_PERSIST_KV append ALREADY EXISTS (don't rebuild) + capacity numbers

SCOPED O(1) conversation KV. KEY ANTI-REBUILD FINDING: the cross-turn O(1) append ALREADY EXISTS = SP_PERSIST_KV (routes.rs run_kvdecode_chat, default-off): LCP-reuse of KV_COMMITTED + suffix-only prefill + bounded-tail rewind (REWIND_BOUND=32, SWA journal Jmax=64); byte-exact null floor when off; excludes replay/single_entry/inject/agency-writers/speculative-recall; auto_recall allowed (non-committing). The persistent-decode verb sp_session_register_kvdecode_backend is registered+gated (G-WIRE-CUDA-DECODE-GEMMA4 32/32 bit-id, VRAM flat). DON'T rebuild the append. Per-position KV = 4KB/owner-layer (global n_kv1*hd512, SWA n_kv2*hd256 both =512 floats K+V); kvfs=48 owners (gemma3 no KV-sharing); 8 global + 40 SWA (period6). Full-cache 192KB/pos; Pmax default 4096 (786MB). SWA ring (SP_DAEMON_KVDECODE_RING_W) bounds the 40 SWA layers O(1); 8 GLOBAL layers are the O(n) floor (evict via existing XBAR Ring-2/Optane demotion). Remaining work: gate G-PERSIST-KV (multi-turn byte-identity + tok/s) then enable; P2 ring+bigger Pmax (~40K tok); P3 global eviction. Coder delegate is CPU L1 (own session, no g_w) = isolated from the Gemma GPU cache.

Full context: [full/55e022c67fb00d30.md](../full/55e022c67fb00d30.md)
